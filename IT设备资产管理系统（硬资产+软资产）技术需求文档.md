# IT设备资产管理系统（硬资产+软资产）技术需求文档

# 1. 文档概述

## 1.1 文档目的

本文档明确IT设备资产管理系统（含硬资产、软资产）的技术架构、功能模块、技术选型、接口设计、安全要求、部署要求等核心技术需求，作为系统开发、测试、部署、维护的唯一技术依据，确保开发过程规范、高效，最终交付符合企业需求的可落地系统。

## 1.2 文档范围

本文档覆盖系统全技术链路，包括：系统架构设计、技术栈选型及说明、硬资产/软资产核心模块技术需求、数据库设计、接口设计（前端-后端、后端-AI助手、后端-流程引擎）、安全需求、部署需求、测试需求、维护需求等，涵盖开发全生命周期的技术规范。

## 受众

- 开发人员：前端（Next.js）、后端（Rust）、数据库开发人员，明确开发标准和实现要求；

- 测试人员：明确测试范围、测试用例设计依据、测试标准；

- 部署人员：明确部署环境、部署步骤、配置要求；

- 维护人员：明确系统维护要点、故障排查方向、版本迭代规范；

- 产品人员：确认技术实现与产品需求的一致性，提供技术可行性参考。

## 参考文档

- 《IT设备资产管理系统产品需求文档》（硬资产+软资产）；

- Tauri 2.0 官方开发文档；

- Next.js 15 官方开发文档；

- Rust 官方文档、sqlx文档；

- PostgreSQL 14+ 官方文档、SQLite 官方文档；

- IronClaw 官方集成文档、n8n 官方开发文档。

# 2. 系统概述

## 2.1 系统定位

本系统是一款企业级IT设备资产管理桌面应用，基于Tauri+Next.js+Rust技术栈开发，支持硬资产、软资产的全生命周期管理，实现资产台账规范化、状态实时同步、流程自动化、查询智能化，同时集成IronClaw AI助手和n8n流程引擎，提升管理效率和智能化水平，保障资产数据安全可控。

## 核心功能概述

系统核心覆盖硬资产、软资产两大板块，实现“采购-入库-分配-使用-维护-报废”全流程管理，具体功能包括：

- 资产基础管理：硬资产/软资产的增删改查、资产编号自动生成、资产分类管理；

- 硬资产管理：实物设备的SN/IP/MAC管理、照片上传、二维码生成、维保管理、状态变更；

- 软资产管理：License授权管理、绑定设备/账号、到期提醒、续约管理、授权分配；

- 流程管理：资产领用/归还/调拨/维修/报废审批、自动流程触发（n8n集成）；

- 智能管理：AI助手查询资产、生成统计报表、异常预警（IronClaw集成）；

- 统计分析：资产分布、状态统计、维保/ License到期统计、成本统计；

- 系统配置：数据库配置、权限管理、分类/状态配置、日志管理。

## 系统边界

### 纳入范围

- 硬资产：计算机设备、网络设备、外设、移动设备、机房硬件等实物资产；

- 软资产：操作系统、办公软件、开发软件、安全软件、云资源、授权许可、账号服务等无形资产；

- 集成模块：IronClaw AI助手（智能查询、流程辅助）、n8n流程引擎（自动化提醒、审批流程）；

- 数据存储：PostgreSQL（业务数据）、SQLite（本地配置数据）；

- 终端适配：Windows 10+、macOS 12+、Linux（Ubuntu 20.04+）。

### 排除范围

- 硬件设备的实时监控（如CPU/内存使用率），仅记录设备配置信息；

- 软资产的安装部署，仅管理授权和绑定信息；

- 第三方支付功能（采购付款流程仅记录状态，不对接支付系统）；

- 跨企业资产共享管理，仅支持单企业内部资产管理。

# 3. 技术架构设计

## 整体架构

系统采用分层架构，清晰划分前端层、桌面容器层、后端服务层、数据存储层、集成层，各层职责明确、低耦合，便于开发、维护和扩展，整体架构如下（从上层到下层）：

前端层（Next.js）→ 桌面容器层（Tauri）→ 后端服务层（Rust）→ 数据存储层（PostgreSQL+SQLite）→ 集成层（IronClaw+ n8n）

## 各层架构说明

### 前端层（Next.js 15）

基于Next.js 15（App Router）开发，采用React组件化开发模式，结合Tailwind CSS实现自适应布局，负责用户交互界面的渲染和用户操作的收集，通过Tauri的IPC通信与后端进行数据交互，核心职责：

- 页面渲染：实现所有业务页面的展示，包括仪表盘、硬资产列表/详情、软资产列表/详情、审批流程等；

- 用户交互：收集用户操作（新增、编辑、删除、查询、审批等），提交至后端处理；

- 数据展示：接收后端返回的数据，以表格、图表、表单等形式展示，支持分页、筛选、排序；

- 本地交互：支持文件上传（资产照片、授权证书）、二维码生成与展示；

- 异常处理：前端表单验证、接口请求异常提示、用户操作反馈。

### 桌面容器层（Tauri 2.0）

作为前端与后端的桥梁，将Next.js前端打包为跨平台桌面应用，提供原生桌面应用体验，核心职责：

- IPC通信：实现前端与Rust后端的双向数据交互，转发前端请求、返回后端处理结果；

- 窗口管理：支持窗口缩放、全屏、最小化、关闭等原生桌面操作，适配不同终端屏幕；

- 应用打包：将前端、后端打包为对应系统（Windows/macOS/Linux）的可执行文件，体积控制在10MB以内；

- 本地资源访问：提供本地文件系统访问权限（限制范围），支持资产照片、证书文件的上传与存储；

- 系统集成：适配系统原生通知（如维保/ License到期提醒）。

### 后端服务层（Rust）

基于Rust语言开发，采用Tokio异步运行时，封装业务逻辑、数据库操作、接口暴露等核心能力，核心职责：

- 业务逻辑封装：实现硬资产/软资产的增删改查、状态变更、分配领用、审批流程等核心业务逻辑；

- 数据库操作：通过SeaORM框架，实现PostgreSQL（业务数据）和SQLite（配置数据）的异步操作；

- 接口暴露：通过Tauri Command提供前端调用接口，通过Axum框架暴露HTTP接口（供IronClaw、n8n调用）；

- 权限控制：实现用户角色、权限的校验与管理，控制不同用户的操作范围；

- 异常处理：统一处理数据库异常、接口请求异常、业务逻辑异常，返回标准化错误信息；

- 日志管理：记录系统操作日志、接口调用日志、错误日志，便于故障排查。

### 数据存储层（PostgreSQL+SQLite）

采用双数据库架构，分别承担业务数据和配置数据的存储，确保数据安全、高效访问：

- PostgreSQL（14+）：存储系统核心业务数据，包括硬资产、软资产、分配记录、操作日志等，支持复杂查询、事务操作，保障数据一致性；

- SQLite：存储本地配置数据，包括PostgreSQL数据库连接配置、系统基础配置等，轻量无需独立部署，实现配置持久化（关闭应用不丢失）。

### 集成层（IronClaw + n8n）

实现系统与AI助手、流程引擎的无缝集成，提升系统智能化和自动化水平：

- IronClaw AI助手：通过Axum HTTP接口与后端对接，实现自然语言查询资产、流程辅助、异常预警等功能；

- n8n流程引擎：通过Webhook接口与后端对接，实现维保/ License到期提醒、审批流程自动化、资产盘点报表生成等功能。

## 架构优势

- 跨平台：基于Tauri，支持Windows、macOS、Linux三大系统，适配企业不同终端环境；

- 高性能：Rust语言保障后端处理高效、安全，Tokio异步运行时提升并发处理能力；

- 低耦合：分层架构设计，各层独立开发、维护，便于后续功能扩展和版本迭代；

- 安全可控：本地私有化部署，数据不泄露；IronClaw沙箱隔离、接口权限控制，保障系统安全；

- 可扩展：预留接口，支持后续集成更多第三方工具（如设备监控、财务系统）。

# 4. 技术栈选型及说明

## 核心技术栈选型

|模块|技术选型|版本要求|选型说明|
|---|---|---|---|
|桌面容器|Tauri|2.0|轻量、跨平台，打包体积小（5-10MB），支持IPC通信，适配桌面端原生体验，比Electron更轻量、更安全|
|前端框架|Next.js|15（App Router）|React生态成熟，开发高效，支持SSR/SSG，适配桌面端自适应布局，路由管理清晰|
|前端样式|Tailwind CSS|3.x|原子化CSS，开发高效，支持响应式布局，适配不同窗口大小，样式统一易维护|
|后端语言|Rust|1.70+|安全、高性能，内存安全无GC，适合系统级开发和数据库操作，与Tauri兼容性好|
|异步运行时|Tokio|1.0+|Rust生态主流异步运行时，支撑数据库异步操作、Tauri异步命令、Axum接口并发|
|ORM框架|SeaORM|1.0+|Rust生态优秀ORM框架，类型安全，支持PostgreSQL和SQLite，实现工具化数据库操作|
|Web框架|Axum|0.7+|轻量、高性能，支持WebSocket、Webhook，用于暴露接口供IronClaw、n8n调用|
|业务数据库|PostgreSQL|14+|稳定、可靠，支持复杂查询、事务操作，适合企业级业务数据存储，扩展性强|
|配置数据库|SQLite|3.x|轻量、无需独立部署，适合存储本地配置数据，实现配置持久化，访问速度快|
|AI助手|IronClaw|最新稳定版|开源、本地优先、安全可控，支持多模型集成，与Rust技术栈契合，适合企业隐私需求|
|流程引擎|n8n|最新稳定版|开源、可视化流程编排，支持400+节点，可实现自动化提醒、审批流程，易于集成|
|测试工具|Tokio Test、Serial Test|最新版|支持Rust异步单元测试，确保数据库操作、业务逻辑、接口的正确性|
|打包工具|Tauri CLI|最新版|一键打包跨平台可执行文件，支持Windows（.exe）、macOS（.dmg）、Linux（.deb/.rpm）|
## 技术栈兼容性要求

- 前端：Next.js 15 需兼容Node.js 18+，Tailwind CSS需与Next.js App Router适配；

- 后端：Rust 1.70+ 需与Tokio 1.0+、SeaORM 1.0+、Axum 0.7+ 版本兼容；

- 数据库：PostgreSQL 14+ 需支持SeaORM 1.0+ 的所有ORM操作，SQLite 3.x 需支持持久化存储；

- 集成工具：IronClaw 需支持HTTP接口调用，n8n 需支持Webhook接口，与Axum接口兼容；

- 终端：Tauri 2.0 需适配Windows 10+、macOS 12+、Linux（Ubuntu 20.04+）的系统环境。

# 5. 核心模块技术需求

## 公共基础模块

### 系统初始化模块

技术需求：

- 应用启动时，自动检测SQLite配置库是否存在，若不存在则自动创建（路径：应用配置目录/config.db）；

- 自动检测PostgreSQL连接配置，若配置不存在，跳转至数据库配置页面；若配置存在，自动尝试连接，连接失败则提示用户重新配置；

- 首次启动时，自动初始化PostgreSQL业务表（assets、hard_assets、soft_assets等），若表已存在则跳过；

- 支持配置备份与恢复，备份文件为加密格式，防止配置泄露。

### 权限管理模块

技术需求：

- 实现角色分级：普通用户、部门管理员、IT管理员、财务、审计，不同角色对应不同操作权限；

- 权限控制粒度：按模块（硬资产、软资产、流程、配置）和操作（增删改查、审批）划分；

- 用户认证：支持本地账号密码登录（密码加密存储，采用bcrypt算法），支持记住密码（加密存储在本地）；

- 权限校验：后端接口需校验用户角色权限，前端页面需根据用户角色隐藏/显示操作按钮；

- 支持角色、权限的动态配置（仅IT管理员可操作）。

### 日志管理模块

技术需求：

- 日志类型：操作日志、接口调用日志、错误日志、系统日志；

- 操作日志：记录用户操作（操作人、操作时间、操作类型、操作对象、操作内容、操作结果）；

- 错误日志：记录系统异常（异常时间、异常位置、异常信息、堆栈信息），便于故障排查；

- 日志存储：操作日志、错误日志存储在PostgreSQL，系统日志存储在本地文件；

- 日志查询：支持按时间范围、操作人、操作类型、日志级别查询日志，支持日志导出（Excel格式）；

- 日志留存：操作日志留存1年，错误日志留存3年，系统日志留存6个月，支持自动清理过期日志。

## 硬资产管理模块

### 资产基础操作

技术需求：

- 新增硬资产：支持表单录入（资产分类、名称、厂商、型号、SN、IP、MAC等），支持资产照片上传（格式：jpg/png，大小≤5MB），自动生成唯一资产编号（规则：部门缩写+年份+序号，如IT-2024-001）；

- 编辑硬资产：支持修改资产信息（除资产编号、SN外，其他字段可修改），支持重新上传资产照片；

- 删除硬资产：支持单个/批量删除，删除前需校验权限（仅IT管理员可删除），删除时同步删除关联的分配记录、操作日志；

- 查询硬资产：支持分页、筛选（按部门、状态、资产分类、采购日期等）、搜索（按资产名称、资产编号、SN）、排序（按采购日期、状态）；

- 资产详情：展示硬资产所有信息（基础信息、配置信息、维保信息、分配记录、操作日志），支持生成资产二维码（包含资产编号、SN等核心信息），支持二维码打印。

### 资产状态管理

技术需求：

- 支持状态：库存、在用、借用、维修、闲置、报废、处置中；

- 状态变更：支持单个/批量变更资产状态，变更时需填写操作人、备注，自动记录操作日志；

- 状态校验：报废、处置中状态不可逆向变更，维修状态可变更为在用/闲置，借用状态可变更为归还（在用/库存）；

- 闲置资产：闲置超过3个月的资产，自动标记为“长期闲置”，在仪表盘展示，提醒管理员处理。

### 维保管理

技术需求：

- 维保信息录入：支持录入维保厂商、维保方式（上门/寄修）、维保到期日期；

- 维保提醒：维保到期前90天（预警）、30天（提醒）、当天（告警），通过系统原生通知+ n8n自动推送（钉钉/企业微信/邮件）提醒管理员；

- 维保记录：记录维保时间、维保内容、维保结果、维保人员，关联对应硬资产，支持查询维保历史。

## 软资产管理模块

### 资产基础操作

技术需求：

- 新增软资产：支持表单录入（资产分类、名称、厂商、版本、授权类型、授权数量、License密钥等），支持授权证书上传（格式：pdf/jpg/png，大小≤10MB），自动生成唯一资产编号；

- 编辑软资产：支持修改软资产信息（除资产编号外，其他字段可修改），支持重新上传授权证书；

- 删除软资产：支持单个/批量删除，删除前需校验权限（仅IT管理员可删除），删除时同步删除关联的分配记录、操作日志；

- 查询软资产：支持分页、筛选（按部门、状态、授权类型、到期日期等）、搜索（按资产名称、资产编号、License密钥）、排序（按到期日期、授权数量）；

- 资产详情：展示软资产所有信息（基础信息、授权信息、绑定信息、分配记录、操作日志），支持查看授权证书，支持License密钥加密展示（隐藏部分字符）。

### 授权管理

技术需求：

- 授权类型：支持订阅、永久、OEM、开源四种类型，开源类型无需录入License密钥；

- 授权分配：支持将软资产授权分配给用户/设备，记录分配时间、分配人、绑定设备/账号，支持批量分配；

- 授权回收：支持回收已分配的授权，回收后授权可重新分配，自动记录回收日志；

- 授权使用率统计：自动计算软资产授权使用率（已分配数量/总授权数量），在仪表盘展示。

### 到期提醒

技术需求：

- 到期预警：License到期前30天（预警）、15天（提醒）、当天（告警），通过系统原生通知+ n8n自动推送提醒管理员；

- 到期处理：支持到期续约（修改到期日期）、停用（变更状态为“过期”），自动记录处理日志；

- 过期处理：过期的软资产自动变更状态为“过期”，禁止分配，提醒管理员续约或停用。

## 资产分配与流程模块

### 资产分配/领用

技术需求：

- 领用流程：用户申请领用→部门管理员审批→IT管理员分配→用户确认领用，自动变更资产状态为“在用/借用”；

- 归还流程：用户申请归还→IT管理员确认→自动变更资产状态为“库存/闲置”，记录归还日期；

- 调拨流程：部门间调拨→调出部门管理员审批→调入部门管理员确认→IT管理员执行调拨，更新资产所属部门；

- 流程记录：记录所有分配/领用/归还/调拨记录，包括申请人、审批人、操作时间、操作结果，支持查询流程历史；

- 审批提醒：通过n8n自动推送审批提醒（钉钉/企业微信/邮件），未审批超过24小时的流程，再次提醒审批人。

### 资产维修/报废流程

技术需求：

- 维修流程：用户申请维修→IT管理员确认→维修中（变更状态）→维修完成（变更状态为在用/闲置）→用户确认，记录维修内容、维修时间；

- 报废流程：用户申请报废→部门管理员审批→IT管理员审核→财务审核→报废确认（变更状态为“报废”）→处置，记录报废原因、处置方式；

- 流程校验：报废流程需经过财务审核，确保资产折旧、处置合规；

- 处置记录：记录报废资产的处置时间、处置方式、处置人，支持查询处置历史。

## 统计分析模块

技术需求：

- 仪表盘展示：直观展示资产总数（硬/软占比）、部门资产分布、资产状态分布、维保即将到期数量、License即将到期数量、闲置资产数量；

- 统计报表：支持生成硬资产统计报表、软资产统计报表、资产分配报表、维保报表、License报表，支持按时间范围（日/周/月/年）筛选；

- 图表展示：采用ECharts实现饼图（资产占比）、柱状图（部门分布）、折线图（资产新增/报废趋势），支持图表导出（png格式）；

- 数据导出：所有报表支持导出为Excel格式，支持批量导出资产台账；

- 自动盘点：每月最后一天自动盘点所有资产状态，生成盘点报表，标记异常资产（如状态异常、未分配资产），提醒管理员核对。

## 集成模块（IronClaw + n8n）

### IronClaw AI助手集成

技术需求：

- 接口对接：通过Axum HTTP接口与IronClaw对接，暴露资产查询、状态更新、流程查询等接口；

- 模型适配：支持接入本地LLM（Llama 4 Scout/DeepSeek），IronClaw配置中指定模型路径，无需训练，通过提示词引导模型解析指令；

- AI功能：支持自然语言查询资产（如“查询IT部门闲置电脑”）、状态更新（如“将资产编号IT-2024-001改为在用”）、流程咨询（如“如何申请资产报废”）、异常预警；

- 安全控制：IronClaw仅能访问指定Axum接口，启用WASM沙箱，限制文件系统访问，API密钥加密存储，定期轮换；

- 日志记录：记录IronClaw接口调用日志，包括调用时间、调用接口、参数、返回结果，便于故障排查。

### n8n流程引擎集成

技术需求：

- 接口对接：通过Axum Webhook接口与n8n对接，支持n8n调用系统接口（查询资产、更新状态），系统触发n8n流程（如状态变更、到期提醒）；

- 自动化流程：实现维保到期提醒、License到期提醒、审批提醒、资产盘点报表推送、异常资产通知等流程；

- 流程配置：提供n8n流程模板（可直接导入），支持用户根据企业需求修改流程节点；

- 异常处理：n8n流程执行失败时，自动记录错误日志，推送提醒给IT管理员，支持流程重试。

# 6. 数据库设计

## 数据库整体设计

采用双数据库架构，PostgreSQL存储业务数据，SQLite存储配置数据，数据库命名规范：表名小写，字段名小写，采用下划线分隔，主键统一命名为id，外键命名为关联表名+_id。

## SQLite配置库设计

### 表名：db_config（数据库连接配置表）

|字段名|字段类型|是否非空|默认值|备注|
|---|---|---|---|---|
|id|INTEGER|是|1|主键，唯一一条配置记录|
|host|TEXT|是|无|PostgreSQL主机地址|
|port|INTEGER|是|5432|PostgreSQL端口|
|db_name|TEXT|是|无|PostgreSQL数据库名|
|username|TEXT|是|无|PostgreSQL用户名|
|password|TEXT|是|无|加密存储，PostgreSQL密码|
|updated_at|TIMESTAMP|是|CURRENT_TIMESTAMP|配置更新时间|
### 表名：system_config（系统基础配置表）

|字段名|字段类型|是否非空|默认值|备注|
|---|---|---|---|---|
|id|INTEGER|是|自增|主键|
|config_key|TEXT|是|无|配置键（如asset_no_rule、alert_days）|
|config_value|TEXT|是|无|配置值|
|remark|TEXT|否|无|配置说明|
|updated_at|TIMESTAMP|是|CURRENT_TIMESTAMP|配置更新时间|
## PostgreSQL业务库设计

### 表名：assets（资产主表，硬资产+软资产通用）

|字段名|字段类型|是否非空|默认值|备注|
|---|---|---|---|---|
|id|SERIAL|是|自增|主键，唯一标识一条资产记录|
|asset_no|VARCHAR(50)|是|无|资产唯一编号，规则：部门缩写+年份+序号（如IT-2024-001），硬/软资产通用|
|asset_type|VARCHAR(20)|是|无|资产类型，区分硬资产/软资产，取值：hardware（硬资产）、software（软资产）|
|category_id|INTEGER|是|无|外键，关联资产分类表（asset_category）id，区分具体资产类别（如电脑、办公软件）|
|name|VARCHAR(100)|是|无|资产名称（如联想ThinkPad X1、Office 365）|
|manufacturer|VARCHAR(100)|是|无|资产厂商（如联想、微软、华为）|
|model|VARCHAR(100)|否|无|资产型号（如X1 Carbon 2024、Office 365 家庭版），软资产可留空|
|department_id|INTEGER|是|无|外键，关联部门表（department）id，标识资产所属部门|
|status|VARCHAR(20)|是|库存|资产状态，硬/软资产通用，取值：库存、在用、借用、维修、闲置、报废、处置中、过期|
|purchase_date|DATE|否|无|采购日期，软资产可记录授权采购日期|
|purchase_price|DECIMAL(10,2)|否|0.00|采购单价，单位：元，软资产记录授权采购单价|
|quantity|INTEGER|是|1|资产数量，硬资产默认1，软资产记录授权总数量|
|used_quantity|INTEGER|是|0|已使用数量，软资产记录已分配授权数量，硬资产默认0（单台管理）|
|expire_date|DATE|否|无|到期日期，软资产记录License到期日，硬资产可记录维保到期日（备用）|
|description|TEXT|否|无|资产描述，记录资产详细信息、备注等|
|attachment_url|VARCHAR(255)|否|无|附件路径，硬资产存照片路径，软资产存授权证书路径，多个路径用逗号分隔|
|created_by|INTEGER|是|无|外键，关联用户表（user）id，记录资产创建人|
|created_at|TIMESTAMP|是|CURRENT_TIMESTAMP|资产创建时间|
|updated_by|INTEGER|否|无|外键，关联用户表（user）id，记录资产最后修改人|
|updated_at|TIMESTAMP|是|CURRENT_TIMESTAMP|资产最后修改时间|
|is_deleted|BOOLEAN|是|false|逻辑删除标识，true=已删除，false=未删除，避免物理删除数据|
### 表名：asset_category（资产分类表，关联资产主表）

|字段名|字段类型|是否非空|默认值|备注|
|---|---|---|---|---|
|id|SERIAL|是|自增|主键，唯一标识一条资产分类记录|
|category_name|VARCHAR(50)|是|无|分类名称，如电脑、网络设备、办公软件等，不可重复|
|asset_type|VARCHAR(20)|是|无|关联资产类型，取值：hardware（硬资产）、software（软资产），与资产主表一致|
|parent_id|INTEGER|否|0|父分类ID，0表示一级分类，非0关联本表格id，支持二级分类（如电脑→笔记本电脑）|
|sort|INTEGER|是|0|分类排序，数值越小排序越靠前，用于页面展示排序|
|description|TEXT|否|无|分类描述，说明该分类包含的资产范围、用途等|
|created_by|INTEGER|是|无|外键，关联用户表（user）id，记录分类创建人|
|created_at|TIMESTAMP|是|CURRENT_TIMESTAMP|分类创建时间|
|updated_by|INTEGER|否|无|外键，关联用户表（user）id，记录分类最后修改人|
|updated_at|TIMESTAMP|是|CURRENT_TIMESTAMP|分类最后修改时间|
|is_deleted|BOOLEAN|是|false|逻辑删除标识，true=已删除，false=未删除，避免物理删除数据|
### 表名：hard_assets（硬资产扩展表，关联资产主表）

### 表名：hard_assets（硬资产扩展表，关联资产主表）

|字段名|字段类型|是否非空|默认值|备注|
|---|---|---|---|---|
|id|SERIAL|是|自增|主键，唯一标识一条硬资产扩展记录|
|asset_id|INTEGER|是|无|外键，关联资产主表（assets）id，一对一关联，软删除主表时同步软删除该记录|
|sn|VARCHAR(50)|是|无|硬资产唯一序列号，如电脑、网络设备的SN码，不可重复|
|ip_address|VARCHAR(50)|否|无|IP地址，网络设备、电脑等可分配IP的资产填写，如192.168.1.100|
|mac_address|VARCHAR(50)|否|无|MAC地址，网络设备、电脑等网络设备填写，如00:1A:2B:3C:4D:5E|
|location|VARCHAR(100)|否|无|资产存放位置，如XX办公楼302室、机房A区01柜|
|maintenance_vendor|VARCHAR(100)|否|无|维保厂商，如联想售后、华为维保，关联维保管理|
|maintenance_type|VARCHAR(20)|否|无|维保方式，取值：上门维保、寄修、远程维保|
|maintenance_expire_date|DATE|否|无|维保到期日期，用于维保到期提醒，优先级高于主表expire_date|
|hardware_config|TEXT|否|无|硬件配置详情，如CPU、内存、硬盘、显卡等，JSON格式存储（如{"cpu":"i7-13700H","memory":"16GB"}）|
|use_user_id|INTEGER|否|无|外键，关联用户表（user）id，记录当前使用人，状态为“在用/借用”时必填|
|use_start_date|DATE|否|无|使用开始日期，状态变更为“在用/借用”时自动记录|
|fault_desc|TEXT|否|无|故障描述，状态为“维修”时填写，记录故障详情|
|created_at|TIMESTAMP|是|CURRENT_TIMESTAMP|扩展记录创建时间，与主表created_at保持一致|
|updated_at|TIMESTAMP|是|CURRENT_TIMESTAMP|扩展记录最后修改时间，与主表updated_at同步更新|
|is_deleted|BOOLEAN|是|false|逻辑删除标识，与主表is_deleted同步，true=已删除，false=未删除|
### 表名：soft_assets（软资产扩展表，关联资产主表）

|字段名|字段类型|是否非空|默认值|备注|
|---|---|---|---|---|
|id|SERIAL|是|自增|主键，唯一标识一条软资产扩展记录|
|asset_id|INTEGER|是|无|外键，关联资产主表（assets）id，一对一关联，软删除主表时同步软删除该记录|
|license_key|VARCHAR(100)|否|无|License密钥，订阅/永久/OEM类型必填，开源类型留空，存储时加密处理|
|license_type|VARCHAR(20)|是|无|授权类型，取值：subscription（订阅）、permanent（永久）、oem（OEM）、open_source（开源）|
|license_period|VARCHAR(50)|否|无|授权期限，订阅类型填写（如1年、3年），永久类型填写“永久”，开源类型留空|
|authorize_scope|VARCHAR(50)|否|无|授权范围，如“企业内所有员工”“IT部门专用”“指定设备”|
|bind_type|VARCHAR(20)|否|无|绑定类型，取值：device（绑定设备）、account（绑定账号）、none（无绑定）|
|bind_info|TEXT|否|无|绑定信息，JSON格式存储，如{"device_ids":[1,2],"account_ids":[3,4]}，无绑定时留空|
|renewal_record|TEXT|否|无|续约记录，JSON格式存储，如[{"renewal_date":"2025-01-01","period":"1年","operator":1}]，记录所有续约信息|
|version|VARCHAR(50)|否|无|软件版本，如Office 365 2024、Photoshop 25.0|
|download_url|VARCHAR(255)|否|无|软件下载地址，可选填，便于用户下载安装|
|authorize_contact|VARCHAR(100)|否|无|授权联系人，记录负责该软资产授权的人员姓名及联系方式|
|created_at|TIMESTAMP|是|CURRENT_TIMESTAMP|扩展记录创建时间，与主表created_at保持一致|
|updated_at|TIMESTAMP|是|CURRENT_TIMESTAMP|扩展记录最后修改时间，与主表updated_at同步更新|
|is_deleted|BOOLEAN|是|### |逻辑删除标识，与主表is_deleted同步，true=已删除，false=未删除|
### 表名：asset_category（资产分类表）示例数据

以下为企业级IT资产管理的标准分类示例，覆盖一级、二级分类，可直接用于系统初始化：

|**分类ID**|**分类名称**|**资产类型**|**父分类ID**|**排序**|**分类描述**|
|---|---|---|---|---|---|
|1|计算机设备|hardware|0|1|核心办公设备，包含台式机、笔记本、一体机等|
|2|笔记本电脑|hardware|1|1|便携型计算机，如ThinkPad、MacBook系列|
|3|台式工作站|hardware|1|2|高性能台式机，用于设计、开发、渲染等专业场景|
|4|网络设备|hardware|0|2|企业网络基础设施，包含交换机、路由器、防火墙等|
|5|核心交换机|hardware|4|1|企业内网核心层交换设备，如华为CE系列、思科C9系列|
|6|无线AP|hardware|4|2|企业无线网络接入点，如Cisco Meraki、华为AP8130|
|7|服务器设备|hardware|0|3|数据中心核心计算设备，包含机架式、刀片式服务器|
|8|机架式服务器|hardware|7|1|标准机架安装式服务器，如戴尔PowerEdge R750|
|9|存储设备|hardware|0|4|企业数据存储设备，包含磁盘阵列、NAS、磁带库等|
|10|磁盘阵列|hardware|9|1|企业级存储阵列，如华为OceanStor、戴尔EMC Isilon|
|11|外设及终端|hardware|0|5|计算机配套外设，包含显示器、键盘鼠标、打印机等|
|12|办公显示器|hardware|11|1|台式机/笔记本配套显示设备，如27英寸4K显示器|
|13|激光打印机|hardware|11|2|部门级打印设备，用于文档输出|
|14|移动设备|hardware|0|6|企业员工移动办公设备，包含手机、平板等|
|15|智能手机|hardware|14|1|企业配发的智能手机，如iPhone、华为Mate系列|
|16|办公软件|software|0|1|企业日常办公必需软件，如Office、WPS、PDF编辑器|
|17|办公套件|software|16|1|包含文字、表格、演示等全功能办公套件|
|18|开发工具|software|0|2|IT研发部门使用的代码编辑、编译、调试工具|
|19|IDE开发环境|software|18|1|集成开发环境，如Visual Studio、IntelliJ IDEA|
|20|安全软件|software|0|3|企业终端安全防护软件，包含杀毒、防火墙、加密软件|
|21|终端杀毒软件|software|20|1|企业级防病毒软件，如360企业版、卡巴斯基|
