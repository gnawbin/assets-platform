# 多租户数据权限与租户切换设计方案

> 解决多用户并发操作不同租户时的数据隔离问题
> 基于 PostgreSQL Schema 隔离 + 请求级参数传递架构

---

## 一、核心问题：全局变量并发不安全

### 当前架构缺陷

```rust
static CURRENT_SCHEMA: OnceLock<RwLock<String>>;  // 进程级全局变量
```

Tauri 是单进程多请求架构（async 运行时跨线程），所有用户请求共享同一个进程：

```
┌──── 用户A (tenant_b) ────┐
│ 请求1: set_current_schema("tenant_b")  │  → CURRENT_SCHEMA = "tenant_b"
└──────────────────────────────┘
┌──── 用户B (tenant_c) ────┐
│ 请求2: set_current_schema("tenant_c")  │  → CURRENT_SCHEMA = "tenant_c"  ← 覆盖了用户A
└──────────────────────────────┘
┌──── 用户A 下一个请求 ────┐
│ schema_prefix() 返回 "tenant_c."    │  → ❌ 用户A读到了用户B的数据
└──────────────────────────────┘
```

### 解决方案

**废除全局 `CURRENT_SCHEMA`，改为请求级参数传递：**

```
前端每个请求携带 tenant_id
       ↓
后端从请求参数获取 tenant_id → 查出 schema_name
       ↓
传给 service 函数 → schema_prefix(schema) 拼出表名前缀
       ↓
不依赖任何全局变量，完全并发安全
```

---

## 二、架构变更概览

```
┌─────────────────────────────────────────────────┐
│ 前端（Next.js）                                    │
│ - 登录后保存 selectedTenantId                     │
│ - 每个 API 请求自动带上 tenant_id 参数              │
│ - 设置页：租户切换 select                          │
└──────────────────┬──────────────────────────────┘
                   │ 请求: tenant_id=2
                   ▼
┌─────────────────────────────────────────────────┐
│ Command/Route 层                                  │
│ - 解析 tenant_id                                  │
│ - 调用 tenant_service::get_schema(tenant_id)      │
│ - 把 schema: &str 传给 service 函数                │
└──────────────────┬──────────────────────────────┘
                   │ schema = "tenant_b"
                   ▼
┌─────────────────────────────────────────────────┐
│ Service 层                                        │
│ - 函数签名增加 schema: &str                       │
│ - database::schema_prefix(schema)                 │
│ - 所有 SQL 拼上 {schema}.table_name               │
└─────────────────────────────────────────────────┘
```

---

## 三、数据表分类

### A 类：租户隔离的业务表（需要传 schema）

这些表在每个租户 schema 下独立存在，**所有 SQL 都需要通过 `schema_prefix(schema)` 拼接前缀**。

| # | schema 下的表 | Service 文件 | 功能 |
|---|-------------|-------------|------|
| 1 | `asset_category` | `assets_categories_service.rs` | 资产分类 |
| 2 | `assets` | `assets_service.rs` | 资产主表 |
| 3 | `hard_assets` | `assets_service.rs` | 硬件资产扩展 |
| 4 | `intangible_assets` | `assets_service.rs` | 无形资产扩展 |
| 5 | `asset_documents` | （暂无 service） | 资产合同文书 |
| 6 | `asset_knowledge` | `knowledge_service.rs` | 知识条目（旧） |
| 7 | `knowledge_tree` | `knowledge_service.rs` | 知识树节点 |
| 8 | `asset_receive` | `process_service.rs` | 领用申请 |
| 9 | `asset_return` | `process_service.rs` | 归还确认 |
| 10 | `asset_transfer` | `process_service.rs` | 调拨记录 |
| 11 | `asset_repair` | `process_service.rs` | 维修记录 |
| 12 | `asset_scrap` | `process_service.rs` | 报废记录 |
| 13 | `asset_purchase` | `process_service.rs` | 采购申请 |
| 14 | `file_uploads` | `upload_service.rs` / `upload_routes.rs` | 文件上传记录 |
| 15 | `knowledge_asset` | `knowledge_asset_service.rs` | OKF 知识资产 |

**涉及的 Rust 文件（5 个 service + 1 个 route）：**

```
src-tauri/src/service/
├── assets_categories_service.rs   → 需改 5 处函数签名
├── assets_service.rs              → 需改 9 处函数签名（刚改完schema_prefix）
├── knowledge_service.rs           → 需改 10 处函数签名
├── process_service.rs             → 需改 24 处函数签名
├── knowledge_asset_service.rs     → 需改 8 处函数签名
src-tauri/src/api/
└── upload_routes.rs               → 需改（当前直接get_current_schema()）
```

### B 类：全局系统表（不需要传 schema，硬编码 `public.`）

这些表在 `public` schema 下，所有租户共享，**不需要 schema 参数**。

| # | 表名 | Service 文件 | 功能 |
|---|------|-------------|------|
| 1 | `public.sys_tenant` | `tenant_service.rs` | 租户配置 |
| 2 | `public.sys_user` | `user_service.rs` / `register_service.rs` | 用户账号 |
| 3 | `public.sys_user_tenant` | （新增）`user_service.rs` | 用户-租户关联 |
| 4 | `public.sys_role` | `role_service.rs` | 角色 |
| 5 | `public.sys_role_menu` | `role_service.rs` | 角色-菜单 |
| 6 | `public.sys_user_role` | `user_service.rs` / `role_service.rs` | 用户-角色 |
| 7 | `public.sys_menu` | `role_service.rs` | 系统菜单 |
| 8 | `public.sys_registration` | `register_service.rs` | 注册申请 |

---

## 四、database 层改动

### 4.1 database/mod.rs

```rust
/// 获取 schema 前缀
///
/// - 如果 schema 为 "public" 或空，返回空字符串（public 下的表不需要前缀）
/// - 否则返回 "{schema}." 格式的前缀
pub fn schema_prefix(schema: &str) -> String {
    if schema == "public" || schema.is_empty() {
        String::new()
    } else {
        format!("{}.", schema)
    }
}
```

**向后兼容处理：**
- 删除 `get_current_schema()` 和 `set_current_schema()` 全局函数
- 删除 `CURRENT_SCHEMA` 全局变量
- `schema_prefix()` 原来的无参版本可以保留但标记 `#[deprecated]`，或者直接移除

### 4.2 postgres.rs

删除以下内容：
```rust
static CURRENT_SCHEMA: OnceLock<RwLock<String>> = OnceLock::new();
pub fn get_current_schema() -> String { ... }
pub fn set_current_schema(schema: &str) { ... }
```

新增：
```rust
/// 根据租户ID获取 schema 名称（用于请求级解析）
pub async fn get_schema_by_tenant_id(pool: &PgPool, tenant_id: i64) -> Result<String> {
    if tenant_id == 1 {
        return Ok("public".to_string());
    }
    let schema = sqlx::query_scalar::<_, String>(
        "SELECT schema_name FROM public.sys_tenant WHERE id = $1 AND enable = true"
    )
    .bind(tenant_id)
    .fetch_optional(pool)
    .await?
    .ok_or_else(|| anyhow!("租户不存在或已禁用"))?;
    Ok(schema)
}
```

---

## 五、Service 层改动模式

### 5.1 业务 service 函数签名改动

**改造前（当前）：**
```rust
pub async fn get_categories() -> Result<Vec<AssetCategory>, String> {
    let pool = database::get_read_pool()...;
    let prefix = database::schema_prefix();  // 从全局变量读
    let sql = format!("SELECT ... FROM {}asset_category", prefix);
    ...
}
```

**改造后：**
```rust
pub async fn get_categories(schema: &str) -> Result<Vec<AssetCategory>, String> {
    let pool = database::get_read_pool()...;
    let prefix = database::schema_prefix(schema);  // 从参数读
    let sql = format!("SELECT ... FROM {}asset_category", prefix);
    ...
}
```

### 5.2 上传路由改动

`upload_routes.rs` 当前直接调用 `get_current_schema()` 获取 schema，改为从请求参数获取：

```rust
// 改造前
let schema = crate::database::postgres::get_current_schema();

// 改造后
let schema = /* 从请求参数或 token 中解析的 tenant_id 查出的 schema */
```

### 5.3 系统 service（B 类表）不需要改

```rust
pub async fn get_tenants() -> Result<Vec<TenantResponse>, String> {
    let pool = get_read_pool()...;
    // 直接查 public.sys_tenant，不需要 schema 参数
    let tenants = sqlx::query_as::<_, SysTenant>(
        "SELECT ... FROM public.sys_tenant ..."
    ).fetch_all(&pool).await...;
}
```

---

## 六、前端改动

### 6.1 请求层

每个 API 请求自动携带当前选中的 `tenant_id`：

```typescript
// api client
const apiClient = {
  async get<T>(path: string, params?: Record<string, any>) {
    const tenantId = getSelectedTenantId(); // 从 store/localStorage 获取
    return axios.get<T>(path, {
      params: { ...params, tenant_id: tenantId },
    });
  },
  // ...
};
```

### 6.2 租户切换

设置页增加租户 select（只有 `available_tenants.length > 1` 时显示）：

```tsx
// 伪代码
const TenantSwitcher = () => {
  const { availableTenants, selectedTenant, switchTenant } = useTenant();
  
  return (
    <Select
      data={availableTenants.map(t => ({ value: t.id, label: t.tenant_name }))}
      value={selectedTenant.id}
      onChange={(tenantId) => switchTenant(tenantId)}
    />
  );
};
```

---

## 七、实施计划

### 阶段 1：database 层清理

| # | 内容 | 文件 |
|---|------|------|
| 1 | 删除 `CURRENT_SCHEMA`、`get_current_schema()`、`set_current_schema()` | `postgres.rs` |
| 2 | 新增 `get_schema_by_tenant_id()` | `postgres.rs` |
| 3 | 将 `schema_prefix()` 改为带参版本 | `database/mod.rs` |

### 阶段 2：业务 service 层改签名

| # | 文件 | 函数数 |
|---|------|--------|
| 1 | `assets_categories_service.rs` | 4 个函数加 `schema: &str` |
| 2 | `assets_service.rs` | 8 个函数加 `schema: &str` |
| 3 | `knowledge_service.rs` | 8 个函数加 `schema: &str` |
| 4 | `process_service.rs` | 18 个函数加 `schema: &str` |
| 5 | `knowledge_asset_service.rs` | 6 个函数加 `schema: &str` |

### 阶段 3：Command/Routes 层传参

| # | 内容 |
|---|------|
| 1 | 所有 command 函数从请求参数取 `tenant_id`，调用 `get_schema_by_tenant_id()` |
| 2 | 将 `schema` 传给对应的 service 函数 |
| 3 | `upload_routes.rs` 同理改造 |

### 阶段 4：前端

| # | 内容 |
|---|------|
| 1 | API client 自动带 `tenant_id` |
| 2 | 设置页租户切换 select |
| 3 | 登录后初始化 `selectedTenantId` |

---

## 八、影响评估

### 优点

- ✅ **彻底解决并发问题** — 不依赖任何全局状态
- ✅ **一次性解决** — 不再需要 `set_current_schema()` 这种运行时切换
- ✅ **`schema_prefix()` 重构成果完全复用** — 只需改成带参数版本
- ✅ **B 类系统表不受影响** — 不改

### 缺点

- ⚠️ **改动面大** — 所有业务 service 函数签名都要加 `&str schema`
- ⚠️ **每个 command/routes 都要解析 tenant_id** — 需要工具函数封装

### 工作量预估

| 阶段 | 内容 | 工时 |
|------|------|------|
| 1 | database 层清理 + 新增函数 | 0.5h |
| 2 | service 层改签名（约 44 个函数） | 2h |
| 3 | command/routes 层传参 | 1h |
| 4 | 前端改动 | 1.5h |
| 5 | 编译验证 + 测试 | 0.5h |
| **总计** | | **5.5h** |