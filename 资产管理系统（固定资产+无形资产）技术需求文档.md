# 资产管理系统（固定资产+无形资产）技术需求文档

# 1. 文档概述

## 1.1 文档目的

本文档明确资产管理系统（含固定资产、无形资产）的技术架构、功能模块、技术选型、接口设计、安全要求、部署要求等核心技术需求，作为系统开发、测试、部署、维护的唯一技术依据，确保开发过程规范、高效，最终交付符合企业需求的可落地系统。

## 1.2 文档范围

本文档覆盖系统全技术链路，包括：系统架构设计、技术栈选型及说明、固定资产/无形资产核心模块技术需求、数据库设计、接口设计（Tauri Command + HTTP API）、安全需求、部署需求、测试需求、维护需求等，涵盖开发全生命周期的技术规范。

## 受众

- 开发人员：前端（Next.js）、后端（Rust）、数据库开发人员，明确开发标准和实现要求；
- 测试人员：明确测试范围、测试用例设计依据、测试标准；
- 部署人员：明确部署环境、部署步骤、配置要求；
- 维护人员：明确系统维护要点、故障排查方向、版本迭代规范；
- 产品人员：确认技术实现与产品需求的一致性，提供技术可行性参考。

## 参考文档

- 《资产管理系统产品需求文档》（固定资产+无形资产）；
- Tauri 2.0 官方开发文档；
- Next.js 16 官方开发文档；
- Rust 官方文档、sqlx 文档；
- PostgreSQL 17+ 官方文档。

# 2. 系统概述

## 2.1 系统定位

本系统是一款企业级资产管理系统桌面应用，基于 Tauri + Next.js + Rust 技术栈开发，支持固定资产、无形资产的全生命周期管理，实现资产台账规范化、状态实时同步、流程自动化、查询智能化，保障资产数据安全可控。

## 核心功能概述

系统核心覆盖固定资产、无形资产两大板块，实现"采购-入库-分配-使用-维护-报废"全流程管理，具体功能包括：

- 资产基础管理：固定资产/无形资产的增删改查、资产编号自动生成（雪花算法）、资产分类管理；
- 固定资产管理：实物设备的 SN/MAC 管理、存放位置管理、维保管理、状态变更；
- 无形资产管理：License 授权管理、绑定设备/账号、到期提醒、续约管理、授权分配、摊销管理；
- 流程管理：资产领用/归还/调拨/维修/报废/采购审批流程；
- 统计分析：资产分布、状态统计、维保/License 到期统计、成本统计；
- 知识库管理：AIFlowy 风格知识树（树形知识库）、知识图谱可视化、RAG 向量检索、知识同步（从资产/合同自动同步）；
- 系统配置：多租户管理、RBAC 权限管理、分类/状态配置、菜单管理。

## 系统边界

### 纳入范围

- 固定资产：计算机设备、网络设备、外设、移动设备、机房硬件等实物资产；
- 无形资产：软件著作权、专利、商标、版权、特许经营权、软件授权等非实物资产；
- 数据存储：PostgreSQL（业务数据），通过 `.env.toml` 配置文件管理连接信息；
- 终端适配：Windows 10+、macOS 12+、Linux（Ubuntu 20.04+）。

### 排除范围

- 硬件设备的实时监控（如 CPU/内存使用率），仅记录设备配置信息；
- 无形资产的安装部署，仅管理授权和绑定信息；
- 第三方支付功能（采购付款流程仅记录状态，不对接支付系统）；
- 跨企业资产共享管理，仅支持单企业内部资产管理。

# 3. 技术架构设计

## 整体架构

系统采用分层架构，清晰划分前端层、桌面容器层、后端服务层、数据存储层，各层职责明确、低耦合，便于开发、维护和扩展，整体架构如下（从上层到下层）：

前端层（Next.js）→ 桌面容器层（Tauri）→ 后端服务层（Rust）→ 数据存储层（PostgreSQL）

## 各层架构说明

### 前端层（Next.js 16）

基于 Next.js 16（App Router）开发，采用 React 组件化开发模式，结合 Mantine UI 组件库实现自适应布局，负责用户交互界面的渲染和用户操作的收集，通过 Tauri 的 IPC 通信或 HTTP API 与后端进行数据交互，核心职责：

- 页面渲染：实现所有业务页面的展示，包括仪表盘、固定资产列表/详情、无形资产列表/详情、审批流程等；
- 用户交互：收集用户操作（新增、编辑、删除、查询、审批等），提交至后端处理；
- 数据展示：接收后端返回的数据，以表格、图表、表单等形式展示，支持分页、筛选、排序；
- 异常处理：前端表单验证、接口请求异常提示、用户操作反馈。

### 桌面容器层（Tauri 2.0）

作为前端与后端的桥梁，将 Next.js 前端打包为跨平台桌面应用，提供原生桌面应用体验，核心职责：

- IPC 通信：通过 Tauri Command 实现前端与 Rust 后端的双向数据交互；
- 窗口管理：支持窗口缩放、全屏、最小化、关闭等原生桌面操作，适配不同终端屏幕；
- 应用打包：将前端、后端打包为对应系统（Windows/macOS/Linux）的可执行文件；
- 系统集成：适配系统原生通知。

### 后端服务层（Rust）

基于 Rust 语言开发，采用 Tokio 异步运行时，封装业务逻辑、数据库操作、接口暴露等核心能力，核心职责：

- 业务逻辑封装：实现固定资产/无形资产的增删改查、状态变更、分配领用、审批流程等核心业务逻辑；
- 数据库操作：通过 sqlx 框架，实现 PostgreSQL 的异步操作，支持读写分离（主库写 + 从库读，加权轮询负载均衡）；
- 接口暴露：通过 Tauri Command 提供前端调用接口，通过 Axum 框架暴露 HTTP RESTful API（支持 OpenAPI/Swagger 文档）；
- 权限控制：实现 RBAC 角色权限校验与管理，基于 JWT 的 HTTP API 认证；
- ID 生成：基于雪花算法（Snowflake）生成全局唯一的 64 位整数 ID；
- 异常处理：统一处理数据库异常、接口请求异常、业务逻辑异常，返回标准化错误信息；
- 日志管理：基于 OpenTelemetry 实现链路追踪和日志收集，支持 OTLP 协议导出。

### 数据存储层（PostgreSQL）

采用 PostgreSQL 14+ 作为唯一业务数据库，支持多租户架构：

- **public schema**：存储全局数据，包括租户配置（sys_tenant）、全局用户（sys_user）、系统菜单（sys_menu）；
- **租户 schema**：每个租户独立 schema，存储业务数据，包括资产主表（assets）、固定资产扩展表（hard_assets）、无形资产扩展表（intangible_assets）、资产分类（asset_category）、部门（sys_department）、角色（sys_role）、流程表等；
- 支持读写分离：主库处理写入操作，从库处理查询操作，从库支持加权轮询负载均衡。

## 架构优势

- 跨平台：基于 Tauri，支持 Windows、macOS、Linux 三大系统，适配企业不同终端环境；
- 高性能：Rust 语言保障后端处理高效、安全，Tokio 异步运行时提升并发处理能力；
- 低耦合：分层架构设计，各层独立开发、维护，便于后续功能扩展和版本迭代；
- 安全可控：本地私有化部署，数据不泄露；JWT 认证 + RBAC 权限控制，保障系统安全；
- 多租户：原生支持多厂区/多租户隔离，数据物理隔离（独立 schema）；
- 可扩展：预留接口，支持后续集成更多第三方工具。

# 4. 技术栈选型及说明

## 核心技术栈选型

| 模块 | 技术选型 | 版本要求 | 选型说明 |
|------|---------|---------|---------|
| 桌面容器 | Tauri | 2.0 | 轻量、跨平台，打包体积小（5-10MB），支持 IPC 通信，适配桌面端原生体验，比 Electron 更轻量、更安全 |
| 前端框架 | Next.js | 16（App Router） | React 生态成熟，开发高效，支持 SSR/SSG，适配桌面端自适应布局，路由管理清晰 |
| 前端 UI | Mantine | 9.x | 功能丰富的 React 组件库，内置表格、表单、弹窗、通知等组件，开发效率高 |
| 后端语言 | Rust | 1.70+ | 安全、高性能，内存安全无 GC，适合系统级开发和数据库操作，与 Tauri 兼容性好 |
| 异步运行时 | Tokio | 1.52+ | Rust 生态主流异步运行时，支撑数据库异步操作、Tauri 异步命令、Axum 接口并发 |
| 数据库驱动 | sqlx | 0.8.6 | Rust 生态优秀异步 SQL 框架，编译时 SQL 检查，支持 PostgreSQL，类型安全 |
| Web 框架 | Axum | 0.7+ | 轻量、高性能，用于暴露 HTTP RESTful API |
| 业务数据库 | PostgreSQL | 14+ | 稳定、可靠，支持复杂查询、事务操作，适合企业级业务数据存储，扩展性强 |
| 密码加密 | Argon2 | 0.5.3 | 业界最强密码哈希算法，内存硬抗 GPU 破解，比 bcrypt 更安全 |
| JWT 认证 | jsonwebtoken | 10.3.0 | 基于 AWS-LC 的 JWT 签发与验证，用于 HTTP API 认证 |
| ID 生成 | twitter_snowflake | 1.0.2 | 雪花算法生成全局唯一 64 位整数 ID，支持自定义 worker_id |
| API 文档 | utoipa + utoipa-swagger-ui | 5.x / 8.x | 自动生成 OpenAPI 3.0 规范文档，提供 Swagger UI 交互式调试 |
| 链路追踪 | OpenTelemetry | 0.32.x | 标准化的可观测性框架，支持 trace/metrics/logs 三支柱，OTLP 协议导出 |
| 序列化 | serde + serde_json | 1.x | Rust 标准序列化框架，支持自定义 i64 ↔ string 序列化（防止前端 JS 精度丢失） |
| 测试工具 | Tokio Test | 最新版 | 支持 Rust 异步单元测试，确保数据库操作、业务逻辑、接口的正确性 |
| 打包工具 | Tauri CLI | 最新版 | 一键打包跨平台可执行文件，支持 Windows（.exe）、macOS（.dmg）、Linux（.deb/.rpm） |

## 技术栈兼容性要求

- 前端：Next.js 16 需兼容 Node.js 20+，Mantine 需与 Next.js App Router 适配；
- 后端：Rust 1.70+ 需与 Tokio 1.52+、sqlx 0.8.6、Axum 0.7+ 版本兼容；
- 数据库：PostgreSQL 17+ 需支持 sqlx 的所有异步操作；
- 终端：Tauri 2.0 需适配 Windows 10+、macOS 12+、Linux（Ubuntu 20.04+）的系统环境。

# 5. 核心模块技术需求

## 公共基础模块

### 系统初始化模块

技术需求：

- 应用启动时，自动加载 `.env.toml` 配置文件，解析为环境变量；
- 自动检测 PostgreSQL 连接配置，若配置不存在或连接失败，提示用户重新配置；
- 首次启动时，自动初始化 public schema 表结构（sys_tenant、sys_user、sys_menu）；
- 首次启动时，自动初始化 public schema 默认数据（默认租户、系统菜单、按钮权限）；
- 首次启动时，自动创建默认租户 schema，初始化租户业务表结构和默认数据（默认角色、部门、角色菜单关联）；
- 支持数据库迁移脚本（public_migration.sql），用于已有旧表的数据库补充缺失列。

### 多租户管理

技术需求：

- 租户配置表（sys_tenant）存储租户名称、对应 schema 名称、启用状态；
- 每个租户拥有独立的 PostgreSQL schema，业务数据物理隔离；
- 全局用户表（sys_user）记录用户所属租户（tenant_id），支持跨租户登录；
- 租户 schema 包含完整的业务表结构：资产表、分类表、部门表、角色表、流程表等。

### 权限管理模块（RBAC）

技术需求：

- 基于 RBAC（Role-Based Access Control）模型，支持角色-菜单-按钮三级权限；
- 菜单表（sys_menu）同时存储菜单路由和按钮权限标识，支持目录/菜单/按钮三种类型；
- 权限标识格式：`模块:功能:操作`（如 `asset:hardware:add`、`system:user:list`）；
- 用户认证：支持本地账号密码登录（密码使用 Argon2 加密存储），HTTP API 使用 JWT Bearer Token 认证；
- 权限校验：后端接口需校验用户角色权限，前端页面根据用户角色动态渲染菜单和按钮；
- 支持角色、权限的动态配置（仅超级管理员可操作）。

### 日志与可观测性

技术需求：

- 基于 OpenTelemetry 实现链路追踪（Tracing）、指标（Metrics）、日志（Logs）三支柱；
- 支持 OTLP 协议导出到 Jaeger、Prometheus、Grafana 等后端；
- 日志级别：支持 trace/debug/info/warn/error 分级，通过环境变量 `RUST_LOG` 控制；
- 日志格式：支持 JSON 格式输出，便于日志收集系统（如 ELK）解析。

## 固定资产管理模块

### 资产基础操作

技术需求：

- 新增固定资产：支持表单录入（资产分类、名称、厂商、型号、SN、MAC 地址等），自动生成唯一资产编号（雪花算法）；
- 编辑固定资产：支持修改资产信息（除资产编号外，其他字段可修改）；
- 删除固定资产：支持单个/批量删除，删除前需校验权限（仅超级管理员可删除），逻辑删除（deleted 标记）；
- 查询固定资产：支持分页、筛选（按部门、状态、资产分类、采购日期等）、搜索（按资产名称、资产编号、SN）、排序；
- 资产详情：展示固定资产所有信息（基础信息、配置信息、维保信息、使用记录）。

### 资产状态管理

技术需求：

- 支持状态：0=正常、1=借用、2=维修、3=报废、4=过期；
- 状态变更：变更时自动记录操作日志；
- 使用人管理：记录当前使用人（use_user_id）和使用开始日期（use_start_date）。

### 维保管理

技术需求：

- 维保信息录入：支持录入维保厂商、维保方式（上门/寄修/远程）、维保到期日期；
- 维保记录：记录维保时间、维保内容、维保结果、维保人员，关联对应固定资产。

## 无形资产管理模块

### 资产基础操作

技术需求：

- 新增无形资产：支持表单录入（资产分类、名称、厂商、版本、无形资产类型等），自动生成唯一资产编号；
- 无形资产类型：software（软件）、patent（专利）、trademark（商标）、copyright（著作权）、franchise（特许经营权）；
- 编辑无形资产：支持修改资产信息（除资产编号外，其他字段可修改）；
- 删除无形资产：支持单个/批量删除，删除前需校验权限，逻辑删除；
- 查询无形资产：支持分页、筛选（按部门、状态、无形资产类型、到期日期等）、搜索、排序；
- 资产详情：展示无形资产所有信息（基础信息、授权信息、绑定信息、摊销信息）。

### 授权管理

技术需求：

- 授权类型：支持 permanent（永久）、subscription（订阅）、device（设备绑定）、user（用户绑定）四种类型；
- 授权分配：支持将无形资产授权分配给用户，记录分配时间、分配人、绑定信息；
- 授权回收：支持回收已分配的授权，回收后授权可重新分配；
- 授权使用率统计：自动计算授权使用率（已分配数量/总授权数量）。

### 摊销管理

技术需求：

- 摊销方法：支持直线摊销法（straight_line），记录摊销方法、使用寿命（年）、月摊销额、残值率；
- 摊销计算：根据资产原值、残值率、使用寿命自动计算月摊销额；
- 摊销记录：记录摊销历史，支持查询已摊销金额和剩余价值。

### 到期提醒

技术需求：

- 到期预警：License/专利到期前自动预警，通过系统原生通知提醒管理员；
- 到期处理：支持到期续约（修改到期日期）、停用（变更状态为"过期"），自动记录处理日志；
- 过期处理：过期的无形资产自动变更状态为"过期"，提醒管理员续约或停用。

## 资产流程管理模块

### 流程类型

系统支持以下 6 种资产流程，每种流程对应独立的数据库表：

| 流程类型 | 表名 | 说明 |
|---------|------|------|
| 领用 | asset_receive | 用户申请领用资产 |
| 归还 | asset_return | 用户归还已领用资产 |
| 调拨 | asset_transfer | 部门间资产调拨 |
| 维修 | asset_repair | 资产维修申请 |
| 报废 | asset_scrap | 资产报废申请 |
| 采购 | asset_purchase | 资产采购申请 |

### 通用审批记录

技术需求：

- 所有流程共用一张审批记录表（asset_approval），通过 biz_type 区分业务类型；
- biz_type：1=领用、2=归还、3=调拨、4=维修、5=报废、6=采购；
- 支持多级审批步骤（step 字段），记录每一步的审批人、审批状态、审批时间、审批备注；
- 审批状态：0=待审、1=同意、2=驳回。

### 领用流程

技术需求：

- 用户申请领用 → 审批 → 确认领用，自动变更资产状态；
- 记录领用单号、领用人、领用部门、领用日期、领用原因；
- 状态流转：0=待审批 → 1=已同意 / 2=已驳回 → 3=已领用 → 4=已归还。

### 归还流程

技术需求：

- 用户申请归还 → 确认归还，记录归还时资产状态（正常/损坏/故障）；
- 关联领用单（receive_id），形成完整的领用-归还闭环；
- 记录归还人、归还日期、确认人、确认时间。

### 调拨流程

技术需求：

- 部门间调拨，记录调出部门、调入部门、调出人、调入人、调拨原因；
- 状态流转：0=待审批 → 1=已调拨 / 2=已驳回。

### 维修流程

技术需求：

- 用户申请维修 → 维修中 → 维修完成 → 用户确认；
- 记录故障描述、维修描述、维修人、维修部门、维修商、维修费用；
- 维修类型：0=送修、1=上门、2=远程；
- 状态流转：0=待维修 → 1=维修中 → 2=已完成 / 3=无法维修。

### 报废流程

技术需求：

- 用户申请报废 → 审批 → 报废确认；
- 记录报废原因、报废日期、处理人；
- 状态流转：0=待审批 → 1=已批准 / 2=已驳回 → 3=已报废。

### 采购流程

技术需求：

- 用户申请采购 → 审批 → 采购中 → 完成；
- 记录采购资产名称、分类、型号、厂商、数量、单价、总价、供应商；
- 状态流转：0=待审批 → 1=采购中 → 2=已完成 / 3=已驳回。

## 统计分析模块

技术需求：

- 仪表盘展示：直观展示资产总数（固定/无形占比）、部门资产分布、资产状态分布、维保即将到期数量、License 即将到期数量；
- 统计报表：支持生成固定资产统计报表、无形资产统计报表、资产分配报表、维保报表、License 报表；
- 数据导出：所有报表支持导出为 Excel 格式，支持批量导出资产台账。

## 知识库管理模块

### 模块概述

知识库管理模块是系统的知识中枢，采用"树形知识库为主、知识图谱为辅、RAG 检索增强"的三位一体设计。树形知识库提供类似 AIFlowy 的文档树管理体验，知识图谱提供节点间的关联探索能力，RAG 检索实现基于向量相似度的智能搜索。

知识库的数据来源包括：
- **手动创建**：用户直接在知识树中创建知识文档；
- **资产同步**：从固定资产/无形资产详情自动同步生成知识条目；
- **合同同步**：从资产文书合同自动同步生成知识条目；
- **批量导入**：支持 Markdown/HTML/TXT 等格式批量导入。

### 知识树管理（AIFlowy 风格）

技术需求：

- **树形结构**：支持无限层级嵌套的树形知识节点，每个节点可以是文件夹（folder）或文档（document）或链接（link）；
- **左侧树导航**：页面左侧为可折叠的树形导航面板，右侧为知识内容编辑/展示区，类似 AIFlowy 的文档树布局；
- **拖拽排序**：支持节点拖拽移动（改变父节点）、拖拽排序（同级节点顺序调整），基于 Mantine Tree 组件实现；
- **节点操作**：支持新增子节点、重命名、删除、复制、移动等操作；
- **节点图标**：支持自定义节点图标（如 📄 文档、📁 文件夹、🔗 链接），默认根据节点类型自动分配；
- **展开/折叠**：支持节点展开/折叠，保存用户的展开状态；
- **搜索过滤**：支持按节点标题搜索，高亮匹配结果，自动展开匹配路径；
- **面包屑导航**：知识文档详情页顶部显示面包屑路径，方便定位层级。

### 知识文档管理

技术需求：

- **富文本编辑**：支持 Markdown 格式编辑和预览，支持代码块、表格、图片嵌入、数学公式等；
- **文档属性**：支持自定义标签（tags）、分类、权限等级（public/internal/secret）；
- **版本历史**：记录文档的修改历史，支持版本对比和回滚；
- **文档关联**：支持关联到资产（asset_id）、合同（document_id），形成知识溯源；
- **自动分块**：大文本自动按段落/标题分块（chunk_index），每块独立存储向量数据；
- **权限控制**：基于 permission_level（public/internal/secret）和 owner_type/owner_id 控制访问权限，对接 RBAC 权限体系。

### 知识图谱

技术需求：

- **节点关联**：支持在知识节点之间建立关联关系，关联类型包括：
  - `related_to`：相关（弱关联）
  - `references`：引用（强关联）
  - `derived_from`：衍生自
  - `belongs_to`：属于
  - `depends_on`：依赖
- **图谱可视化**：使用 D3.js 或 vis-network 在前端渲染力导向图（Force-Directed Graph），支持：
  - 节点拖拽、缩放、平移
  - 点击节点查看详情
  - 高亮关联路径
  - 筛选节点类型
- **自动关联推荐**：基于向量相似度（vector_data 字段的余弦相似度），自动推荐关联节点；
- **图谱布局**：支持力导向布局、层级布局、环形布局等多种可视化布局。

### RAG 检索增强

技术需求：

- **向量化存储**：知识文档内容通过 Embedding 模型生成向量，存储于 `asset_knowledge.vector_data` 字段；
- **语义搜索**：支持基于向量相似度的语义搜索，输入自然语言查询，返回最相关的知识条目；
- **混合搜索**：支持关键词全文检索（PostgreSQL tsvector）+ 向量语义搜索的混合检索模式；
- **检索结果**：返回匹配的知识片段（chunk），包含上下文、来源文档、相似度分数；
- **权限过滤**：检索结果根据用户权限过滤，仅返回用户有权限访问的知识条目。

### 知识同步

技术需求：

- **资产同步**：新增/编辑固定资产或无形资产时，自动同步生成对应的知识条目（knowledge_type 为 hardware/intangible），标题为资产名称，内容为资产详情摘要；
- **合同同步**：新增/编辑资产文书合同时，自动同步生成对应的知识条目（knowledge_type 为 contract），标题为合同名称，内容为合同关键信息；
- **同步策略**：支持自动同步（实时）和手动同步（用户触发），支持同步范围选择（全部/指定资产）；
- **同步日志**：记录每次同步的操作时间、操作人、同步数量、同步结果。

### 前端实现说明

- **树组件**：基于 Mantine Tree 组件实现知识树，通过自定义拖拽逻辑（@dnd-kit 或 react-beautiful-dnd）实现拖拽排序；
- **布局**：左侧树面板（可折叠，宽度可拖拽调整）+ 右侧内容区，适配桌面端大屏体验；
- **富文本编辑器**：集成 Markdown 编辑器（如 react-markdown + @uiw/react-md-editor）；
- **图谱可视化**：使用 vis-network-react 或 react-d3-graph 实现知识图谱渲染；
- **状态管理**：使用 React Context 或 Zustand 管理知识树的展开状态、选中节点、拖拽状态。

## 文档解析与向量化处理

### 处理架构概述

文档解析与向量化采用"Rust 文件管理 + Windmill Python 异步处理"的离线架构。用户上传的文件由 Rust 端（tauri::api::fs）写入本地文件系统并记录元数据，然后通过 Windmill Webhook 触发 Python 异步任务进行文档解析、OCR 识别、文本分块和向量化，最终结果回写入 asset_knowledge 表。

```
┌─────────────────────────────────────────────────────────────────────────┐
│                    文件上传与向量化处理整体架构                           │
├─────────────────────────────────────────────────────────────────────────┤
│                                                                         │
│  ┌──────────┐     ┌──────────────┐     ┌──────────────────────────┐    │
│  │ 用户上传  │────▶│ Rust 端处理  │────▶│ 本地文件系统              │    │
│  │ (前端页面)│     │              │     │ (tauri::api::fs)         │    │
│  └──────────┘     │ • 接收文件   │     └──────────────────────────┘    │
│                   │ • 写入 fs    │                                      │
│                   │ • 记录元数据 │     ┌──────────────────────────┐    │
│                   │ • 触发 Webhook│───▶│ asset_documents 表       │    │
│                   └──────┬───────┘     │ (文件元数据 + 状态)      │    │
│                          │             └──────────────────────────┘    │
│                          │ Webhook 触发                                │
│                          ▼                                             │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │                    Windmill 平台                             │       │
│  │  ┌─────────────┐  ┌─────────────┐  ┌─────────────────────┐  │       │
│  │  │ Webhook API │─▶│ 任务队列     │─▶│ Python Worker 执行  │  │       │
│  │  │ /api/w/...  │  │ (异步/重试)  │  │                     │  │       │
│  │  └─────────────┘  └─────────────┘  │ ① 文件解析          │  │       │
│  │                                    │ ② OCR 识别          │  │       │
│  │  ┌─────────────┐  ┌─────────────┐  │ ③ 文本分块          │  │       │
│  │  │ 定时任务     │  │ 失败重试队列 │  │ ④ Embedding 向量化  │  │       │
│  │  │ (cron 调度) │  │ (死信处理)   │  │ ⑤ 写入知识库        │  │       │
│  │  └─────────────┘  └─────────────┘  └──────────┬──────────┘  │       │
│  └────────────────────────────────────────────────┼─────────────┘       │
│                                                   │                     │
│                                                   ▼                     │
│  ┌─────────────────────────────────────────────────────────────┐       │
│  │                     asset_knowledge 表                        │       │
│  │  (知识条目 + 分块内容 + 向量数据 + 权限信息)                   │       │
│  └─────────────────────────────────────────────────────────────┘       │
│                                                                         │
└─────────────────────────────────────────────────────────────────────────┘
```

### 文件上传与存储

技术需求：

- **文件上传**：用户通过前端页面选择文件上传，支持多文件批量上传；
- **文件存储**：使用 Rust `tauri::api::fs` 将文件写入本地文件系统，存储路径规则为 `{data_dir}/knowledge/{tenant_id}/{uuid}.{ext}`；
- **元数据记录**：文件上传成功后，写入 `asset_documents` 表，记录文件名、文件大小、文件类型、存储路径、上传人、上传时间；
- **文件类型限制**：支持 PDF、DOC/DOCX、PPT/PPTX、TXT、Markdown、HTML、图片（JPG/PNG/BMP/TIFF），单文件最大 50MB；
- **文件状态**：`asset_documents` 表新增 `process_status` 字段，标记文件处理状态：
  - `pending`：待处理
  - `processing`：处理中
  - `completed`：处理完成
  - `failed`：处理失败

### 文件格式解析

| 文件格式 | 解析方案 | Rust 库 / Python 库 | 说明 |
|---------|---------|-------------------|------|
| PDF | PyMuPDF (fitz) | `PyMuPDF` | 文本提取、表格提取、元数据提取 |
| DOC/DOCX | python-docx | `python-docx` | 段落解析、样式保留、表格提取 |
| PPT/PPTX | python-pptx | `python-pptx` | 幻灯片文本提取、备注提取 |
| TXT | 直接读取 | Rust `std::fs` | UTF-8 编码文本 |
| Markdown | 直接读取 | Rust `std::fs` | 保留标题层级用于智能分块 |
| HTML | html2text | `html2text` | 去除 HTML 标签，提取纯文本 |
| 图片（JPG/PNG/BMP/TIFF） | OCR 识别 | PaddleOCR / Tesseract | 见下方 OCR 处理流程 |

### OCR 图片文字识别

技术需求：

- **OCR 引擎**：使用 PaddleOCR（推荐）或 Tesseract OCR 作为备选方案；
- **支持语言**：中文简体、中文繁体、英文（自动检测语言）；
- **图片预处理**：灰度化、二值化（Otsu 算法）、降噪（高斯滤波/中值滤波）、倾斜校正（霍夫变换）、分辨率归一化（300 DPI）；
- **文字检测**：基于 DBnet（Differentiable Binarization）检测文字区域，输出文本框坐标；
- **文字识别**：基于 CRNN（卷积循环神经网络）识别文本框内文字，输出文本内容和置信度；
- **后处理**：拼写校正（基于词典）、段落重组（根据文本框坐标和间距还原阅读顺序）、去除重复空格和异常字符；
- **表格识别**：支持图片中表格结构的识别，输出 Markdown 表格格式。

```
┌─────────────────────────────────────────────────────────────────┐
│                        OCR 处理流程图                            │
├─────────────────────────────────────────────────────────────────┤
│                                                                 │
│  ┌──────────┐   ┌──────────────┐   ┌──────────────────────┐    │
│  │ 输入图片  │──▶│ 图片预处理    │──▶│ 文字区域检测 (DBnet) │    │
│  │ (JPG/PNG)│   │              │   │                      │    │
│  └──────────┘   │ • 灰度化     │   │ • 输出文本框坐标     │    │
│                 │ • 二值化     │   │ • 输出置信度分数     │    │
│                 │ • 降噪       │   └──────────┬───────────┘    │
│                 │ • 倾斜校正   │              │                │
│                 │ • 分辨率归一  │              ▼                │
│                 └──────────────┘   ┌──────────────────────┐    │
│                                    │ 文字识别 (CRNN)      │    │
│                                    │                      │    │
│                                    │ • 逐框识别文字      │    │
│                                    │ • 输出文本+置信度   │    │
│                                    └──────────┬───────────┘    │
│                                               │                │
│                                               ▼                │
│                 ┌──────────────┐   ┌──────────────────────┐    │
│                 │ 输出纯文本    │◀──│ 后处理               │    │
│                 │ (UTF-8)      │   │                      │    │
│                 └──────────────┘   │ • 拼写校正          │    │
│                                    │ • 段落重组          │    │
│                                    │ • 表格结构还原      │    │
│                                    │ • 去除异常字符      │    │
│                                    └──────────────────────┘    │
│                                                                 │
└─────────────────────────────────────────────────────────────────┘
```

### 文本分块策略

技术需求：

- **分块方式**：支持三种分块策略，根据文档类型自动选择：
  1. **Markdown 标题分块**：按 `#` / `##` / `###` 标题层级分割，保留标题作为块元数据（适用于 Markdown/HTML 文档）；
  2. **段落分块**：按空行/段落分割，每块不超过 1024 tokens（适用于 TXT/PDF 纯文本）；
  3. **固定长度分块**：按固定 token 数（512/1024）滑动窗口分割，支持重叠（overlap 10%-20%）（适用于 OCR 识别文本）；
- **块元数据**：每个分块记录以下元数据：
  - `source_document`：来源文档 ID
  - `chunk_index`：块序号
  - `page_number`：页码（PDF/PPT 适用）
  - `title_path`：标题路径（如 "第3章 > 3.1节 > 3.1.1"）
  - `token_count`：块 token 数
- **分块后处理**：过滤空块、过短块（< 10 tokens）、纯标点符号块。

### 向量化处理

技术需求：

- **Embedding 模型**：使用开源本地 Embedding 模型，支持离线运行：
  - 首选：BGE (BAAI/bge-large-zh-v1.5) - 1024 维，中文优化
  - 备选：text2vec-base-chinese - 768 维，轻量级
  - 备选：m3e-base - 768 维，中文优化
- **向量维度**：根据所选模型确定（768 / 1024），统一存储于 `asset_knowledge.vector_data` 字段（BYTEA 类型）；
- **向量生成**：Python 端调用 sentence-transformers 库生成向量，输出为 float32 数组序列化为字节流；
- **向量索引**：PostgreSQL 侧通过 pgvector 插件（可选）或应用层暴力搜索（数据量 < 10 万条时）；
- **批量处理**：支持批量向量化，每批最多 32 个文本块，提高处理效率。

### Windmill 离线处理

技术需求：

- **触发方式**：Rust 端通过 HTTP POST 调用 Windmill Webhook API，传递文件路径和文件 ID；
- **Webhook 请求格式**：
  ```json
  {
      "file_id": 123456789,
      "file_path": "/data/knowledge/tenant_1/abc.pdf",
      "file_type": "pdf",
      "tenant_id": 1,
      "callback_url": "http://localhost:3001/api/knowledge/callback"
  }
  ```
- **Windmill 脚本**：Python 脚本执行以下步骤：
  1. 根据 `file_type` 选择解析器（PDF/DOCX/PPTX/图片/TXT/MD/HTML）；
  2. 执行文件解析，提取纯文本；
  3. 图片文件执行 OCR 识别流程；
  4. 对提取的文本执行分块策略；
  5. 对每个分块调用 Embedding 模型生成向量；
  6. 将分块结果 + 向量数据写入 `asset_knowledge` 表；
  7. 回调 `callback_url` 通知 Rust 端处理完成；
- **定时任务**：Windmill 支持 cron 定时调度，用于：
  - 重试处理失败的文件（process_status = 'failed'）；
  - 批量重新向量化（当 Embedding 模型升级时）；
  - 清理过期临时文件；
- **失败处理**：
  - 处理失败时，Windmill 自动重试 3 次（指数退避）；
  - 超过重试次数后，回调 Rust 端标记为 `failed`；
  - 支持手动触发重新处理。

```
┌─────────────────────────────────────────────────────────────────────┐
│                      Windmill 离线处理流程图                         │
├─────────────────────────────────────────────────────────────────────┤
│                                                                     │
│  ┌──────────┐    ┌──────────────────┐    ┌────────────────────┐    │
│  │ Rust 端   │    │ Windmill Webhook │    │ Windmill Worker    │    │
│  │          │    │                  │    │ (Python 3.11+)     │    │
│  │ ① 文件   │───▶│ ② 接收请求      │───▶│ ③ 执行处理脚本     │    │
│  │   上传   │    │ ③ 加入任务队列   │    │                    │    │
│  │ ② 写入   │    │ ④ 返回任务 ID   │    │ • 文件解析         │    │
│  │   fs     │    └──────────────────┘    │ • OCR 识别         │    │
│  │ ③ 记录   │                            │ • 文本分块         │    │
│  │   元数据 │    ┌──────────────────┐    │ • Embedding 向量化 │    │
│  │ ④ 触发   │    │ 定时任务 (cron)   │    │ • 写入知识库       │    │
│  │   Webhook│    │                  │    └────────┬───────────┘    │
│  └──────────┘    │ • 每 5 分钟扫描  │             │                │
│                  │   失败重试队列   │             │ 回调            │
│  ┌──────────┐    │ • 每日凌晨批量   │             ▼                │
│  │ Rust 端   │    │   重新向量化    │    ┌────────────────────┐    │
│  │ 回调处理  │◀───│ • 每周清理      │    │ Rust 端回调接口    │    │
│  │          │    │   临时文件       │    │                    │    │
│  │ ⑤ 更新   │    └──────────────────┘    │ ⑥ 更新文件状态    │    │
│  │   状态   │                             │ ⑦ 记录处理日志    │    │
│  └──────────┘                             └────────────────────┘    │
│                                                                     │
└─────────────────────────────────────────────────────────────────────┘
```

### 实时处理（备选方案）

技术需求：

- **适用场景**：小文件（< 1MB）或用户要求即时返回结果的场景；
- **实现方式**：Rust 端通过 `std::process::Command` 或 Tauri Sidecar 直接调用 Python 脚本；
- **处理流程**：文件上传 → Rust 调用 Python 子进程 → 同步等待结果 → 直接返回前端；
- **限制**：仅支持 TXT、Markdown、小 PDF 文件，不支持图片 OCR 和大文件；
- **超时控制**：实时处理超时时间 30 秒，超时后自动转为 Windmill 离线处理。

### 技术栈选型补充

| 模块 | 技术选型 | 说明 |
|------|---------|------|
| 文件存储 | Rust `tauri::api::fs` | 本地文件系统存储 |
| 任务调度 | Windmill | 开源 Python 任务调度平台，支持 Webhook + cron |
| PDF 解析 | PyMuPDF (fitz) | 高性能 PDF 文本提取 |
| DOCX 解析 | python-docx | Word 文档解析 |
| PPTX 解析 | python-pptx | PowerPoint 解析 |
| OCR 引擎 | PaddleOCR | 中文 OCR 识别，支持表格识别 |
| Embedding 模型 | BGE (BAAI/bge-large-zh-v1.5) | 中文向量化模型，1024 维 |
| 向量化框架 | sentence-transformers | Python 向量化推理框架 |

# 6. 数据库设计

## 数据库整体设计

采用 PostgreSQL 单数据库多 schema 架构，public schema 存储全局数据，每个租户独立 schema 存储业务数据。

数据库命名规范：表名小写，字段名小写，采用下划线分隔，主键统一命名为 id，外键命名为关联表名+_id。

所有表主键使用雪花算法生成的 64 位整数 ID，支持逻辑删除（deleted 字段：0=未删除，1=已删除）。

## public schema 全局表

### 表名：public.sys_tenant（租户配置表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键，雪花算法 ID |
| tenant_name | VARCHAR(100) | 是 | - | 租户名称 |
| schema_name | VARCHAR(50) | 是 | - | 对应 PostgreSQL schema 名，唯一 |
| enable | BOOLEAN | 是 | true | 是否启用 |
| create_at | TIMESTAMP WITH TIME ZONE | 是 | CURRENT_TIMESTAMP | 创建时间 |

### 表名：public.sys_user（全局登录用户表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键，雪花算法 ID |
| username | VARCHAR(100) | 是 | - | 用户名，唯一 |
| passwd | VARCHAR(255) | 是 | - | 密码，Argon2 加密存储 |
| domain | VARCHAR(100) | 否 | - | 域 |
| real_name | VARCHAR(255) | 是 | - | 真实姓名 |
| email | VARCHAR(255) | 否 | - | 邮箱 |
| phone | VARCHAR(50) | 否 | - | 电话 |
| department_id | BIGINT | 否 | - | 部门 ID |
| status | SMALLINT | 是 | 1 | 状态：1=正常，0=禁用 |
| nickname | VARCHAR(255) | 否 | - | 昵称 |
| avatar | VARCHAR(500) | 否 | - | 头像 |
| person_id | VARCHAR(50) | 否 | - | 身份证号 |
| person_code | VARCHAR(50) | 否 | - | 工号 |
| super_user_id | BIGINT | 否 | - | 上级用户 ID |
| tenant_id | BIGINT | 是 | - | 所属租户 ID，关联 sys_tenant |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | CURRENT_TIMESTAMP | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标志：0=未删除，1=已删除 |

### 表名：public.sys_menu（系统菜单&权限表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| menu_name | VARCHAR(255) | 是 | - | 菜单/按钮名称 |
| parent_id | BIGINT | 否 | - | 父菜单 ID |
| path | VARCHAR(255) | 否 | - | 路由路径（菜单用） |
| component | VARCHAR(255) | 否 | - | 前端组件路径（菜单用） |
| icon | VARCHAR(255) | 否 | - | 图标 |
| order_num | SMALLINT | 是 | - | 排序号 |
| visible | BOOLEAN | 是 | - | 是否可见 |
| perms | VARCHAR(255) | 否 | - | 权限标识（按钮用） |
| menu_type | SMALLINT | 是 | - | 菜单类型：1=目录、2=菜单、3=按钮 |
| hidden_button | BOOLEAN | 是 | - | 是否隐藏按钮 |
| command_name | VARCHAR(255) | 否 | - | Tauri Command 名称 |
| http_method | VARCHAR(10) | 否 | - | HTTP 方法 |
| http_path | VARCHAR(255) | 否 | - | HTTP 路径 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | - | 删除标志 |

## 租户 schema 业务表

### 表名：{schema}.asset_category（资产分类表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| category_name | VARCHAR(255) | 是 | - | 分类名称 |
| asset_type | VARCHAR(100) | 是 | - | 资产类型：fixed=固定资产、intangible=无形资产 |
| parent_id | BIGINT | 是 | - | 父分类 ID，0 表示一级分类 |
| sort | SMALLINT | 是 | - | 排序号 |
| description | TEXT | 否 | - | 分类描述 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 否 | - | 删除标志 |

### 表名：{schema}.assets（资产主表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键，雪花算法 ID |
| asset_no | VARCHAR(100) | 是 | - | 资产编号，唯一 |
| asset_type | VARCHAR(50) | 是 | - | 资产类型：fixed=固定资产、intangible=无形资产 |
| category_id | BIGINT | 是 | - | 资产分类 ID |
| asset_name | VARCHAR(255) | 是 | - | 资产名称 |
| manufacturer | VARCHAR(255) | 否 | - | 制造商/厂商 |
| model | VARCHAR(255) | 否 | - | 型号 |
| department_id | BIGINT | 否 | - | 使用部门 ID |
| user_id | BIGINT | 否 | - | 使用人 ID |
| status | SMALLINT | 是 | 0 | 状态：0=正常、1=借用、2=维修、3=报废、4=过期 |
| purchase_date | TIMESTAMP WITH TIME ZONE | 否 | - | 购买日期 |
| purchase_price | NUMERIC(12,2) | 否 | 0.00 | 购买金额 |
| quantity | INTEGER | 是 | 1 | 总数量 |
| used_quantity | INTEGER | 是 | 0 | 已使用数量 |
| expire_date | TIMESTAMP WITH TIME ZONE | 否 | - | 到期日 |
| description | TEXT | 否 | - | 备注说明 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标记：0=未删除、1=已删除 |

### 表名：{schema}.hard_assets（固定资产扩展表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| asset_id | BIGINT | 是 | - | 关联资产主表 ID，ON DELETE CASCADE |
| sn | VARCHAR(100) | 否 | - | 序列号 SN |
| mac_address | VARCHAR(100) | 否 | - | MAC 地址 |
| location | VARCHAR(255) | 否 | - | 存放位置 |
| hardware_config | TEXT | 否 | - | 硬件配置（JSON 格式） |
| use_user_id | BIGINT | 否 | - | 使用人 ID |
| use_start_date | TIMESTAMP WITH TIME ZONE | 否 | - | 使用开始日期 |
| maintenance_vendor | VARCHAR(255) | 否 | - | 维保厂商 |
| maintenance_type | VARCHAR(100) | 否 | - | 维保类型 |
| maintenance_expire_date | TIMESTAMP WITH TIME ZONE | 否 | - | 维保到期日 |
| fault_desc | TEXT | 否 | - | 故障描述 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标记 |

### 表名：{schema}.intangible_assets（无形资产扩展表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| asset_id | BIGINT | 是 | - | 关联资产主表 ID，ON DELETE CASCADE |
| intangible_type | VARCHAR(50) | 是 | - | 无形资产类型：software/patent/trademark/copyright/franchise |
| register_no | VARCHAR(100) | 否 | - | 注册号/专利号/商标号 |
| register_owner | VARCHAR(255) | 否 | - | 权利人 |
| register_date | TIMESTAMP WITH TIME ZONE | 否 | - | 申请/注册日期 |
| valid_start_date | TIMESTAMP WITH TIME ZONE | 否 | - | 生效开始日期 |
| valid_end_date | TIMESTAMP WITH TIME ZONE | 否 | - | 有效截止日期 |
| right_status | VARCHAR(100) | 否 | - | 权利状态 |
| license_key | VARCHAR(255) | 否 | - | 许可证密钥 |
| license_type | VARCHAR(100) | 否 | - | 许可证类型：permanent/subscription/device/user |
| authorized_scope | VARCHAR(255) | 否 | - | 授权范围 |
| assigned_user_ids | TEXT | 否 | - | 授权用户 ID 集合（逗号分隔） |
| bind_type | VARCHAR(100) | 否 | - | 绑定类型：设备/用户/IP |
| bind_info | TEXT | 否 | - | 绑定信息 |
| version | VARCHAR(100) | 否 | - | 版本号 |
| download_link | VARCHAR(255) | 否 | - | 下载地址 |
| amortization_method | VARCHAR(50) | 否 | straight_line | 摊销方法：straight_line=直线摊销法 |
| useful_life | INTEGER | 否 | - | 使用寿命（年） |
| amortization_amount | NUMERIC(12,2) | 否 | 0.00 | 月摊销额 |
| residual_rate | NUMERIC(5,2) | 否 | 0.05 | 残值率 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标记 |

### 表名：{schema}.asset_documents（资产文书合同表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| asset_id | BIGINT | 是 | - | 关联资产主表 ID，ON DELETE CASCADE |
| doc_type | VARCHAR(50) | 是 | - | 文档类型：contract/agreement/authorization/certificate/record |
| doc_name | VARCHAR(255) | 是 | - | 文档名称 |
| doc_no | VARCHAR(100) | 否 | - | 合同编号/证书编号 |
| party_a | VARCHAR(255) | 否 | - | 甲方 |
| party_b | VARCHAR(255) | 否 | - | 乙方 |
| sign_date | TIMESTAMP WITH TIME ZONE | 否 | - | 签订日期 |
| effective_date | TIMESTAMP WITH TIME ZONE | 否 | - | 生效日期 |
| expire_date | TIMESTAMP WITH TIME ZONE | 否 | - | 到期日期 |
| file_path | TEXT | 否 | - | 文件存储路径 |
| file_name | VARCHAR(255) | 否 | - | 文件原名 |
| file_size | BIGINT | 否 | - | 文件大小（字节） |
| remark | TEXT | 否 | - | 备注 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标记 |

### 表名：{schema}.asset_knowledge（资产知识库表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| asset_id | BIGINT | 是 | - | 关联资产主表 ID |
| doc_source | VARCHAR(50) | 是 | - | 数据来源：asset/hardware/intangible/document |
| knowledge_type | VARCHAR(50) | 是 | - | 知识类型：basic/contract/hardware/intangible |
| title | VARCHAR(255) | 是 | - | 知识标题 |
| content | TEXT | 是 | - | 知识内容（用于向量化检索 + 模型微调） |
| chunk_index | INTEGER | 是 | 0 | 文本分块序号 |
| vector_data | BYTEA | 否 | - | 向量数据 |
| permission_level | VARCHAR(50) | 是 | internal | 权限等级：public/internal/secret |
| owner_type | VARCHAR(50) | 否 | - | 归属类型：user/dept/role |
| owner_id | BIGINT | 否 | - | 归属 ID |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标记 |

### 表名：{schema}.knowledge_tree（知识树节点表）

知识树节点表用于存储 AIFlowy 风格的树形知识库结构，支持无限层级嵌套。

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键，雪花算法 ID |
| parent_id | BIGINT | 否 | 0 | 父节点 ID，0 表示根节点 |
| knowledge_id | BIGINT | 否 | - | 关联 asset_knowledge 表 ID（文档/链接节点必填） |
| node_type | VARCHAR(20) | 是 | folder | 节点类型：folder=文件夹、document=文档、link=链接 |
| title | VARCHAR(255) | 是 | - | 节点标题 |
| icon | VARCHAR(50) | 否 | - | 节点图标（Emoji 或图标名称） |
| sort | INTEGER | 是 | 0 | 同级排序号 |
| is_expanded | BOOLEAN | 是 | true | 是否展开（仅 folder 类型有效） |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标记：0=未删除、1=已删除 |

索引：
- `idx_tree_parent`：parent_id（加速子节点查询）
- `idx_tree_knowledge`：knowledge_id（加速知识关联查询）
- `idx_tree_sort`：(parent_id, sort)（加速同级排序）

### 表名：{schema}.knowledge_relation（知识图谱关联表）

知识图谱关联表用于存储知识节点之间的关联关系，形成知识图谱。

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键，雪花算法 ID |
| source_id | BIGINT | 是 | - | 源节点 ID（关联 knowledge_tree.id） |
| target_id | BIGINT | 是 | - | 目标节点 ID（关联 knowledge_tree.id） |
| relation_type | VARCHAR(30) | 是 | related_to | 关联类型：related_to=相关、references=引用、derived_from=衍生自、belongs_to=属于、depends_on=依赖 |
| weight | NUMERIC(3,2) | 是 | 1.00 | 关联权重（0.00~1.00），用于图谱布局和推荐排序 |
| description | TEXT | 否 | - | 关联描述 |
| auto_generated | BOOLEAN | 是 | false | 是否自动生成（基于向量相似度） |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 是 | NOW() | 更新时间 |
| deleted | SMALLINT | 是 | 0 | 删除标记：0=未删除、1=已删除 |

约束：
- 唯一约束：UNIQUE(source_id, target_id, relation_type)（防止重复关联）
- CHECK：source_id != target_id（禁止自关联）

索引：
- `idx_relation_source`：source_id（加速源节点查询）
- `idx_relation_target`：target_id（加速目标节点查询）
- `idx_relation_type`：relation_type（加速按类型筛选）

### 表名：{schema}.sys_department（部门表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键，雪花算法 ID |
| department_name | VARCHAR(255) | 是 | - | 部门名称 |
| parent_id | BIGINT | 否 | - | 父部门 ID |
| description | TEXT | 否 | - | 部门描述 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 否 | - | 删除标志 |

### 表名：{schema}.sys_role（角色表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键，雪花算法 ID |
| role_key | VARCHAR(100) | 是 | - | 角色标识，唯一 |
| role_name | VARCHAR(100) | 是 | - | 角色名称 |
| description | TEXT | 否 | - | 角色描述 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 否 | - | 删除标志 |

### 表名：{schema}.sys_user_role（用户角色关联表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| user_id | BIGINT | 是 | - | 用户 ID |
| role_id | BIGINT | 是 | - | 角色 ID |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 否 | - | 删除标志 |

### 表名：{schema}.sys_role_menu（角色菜单关联表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| role_id | BIGINT | 是 | - | 角色 ID |
| menu_id | BIGINT | 是 | - | 菜单 ID |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 否 | - | 删除标志 |

### 表名：{schema}.asset_receive（资产领用申请表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| receive_no | VARCHAR(100) | 是 | - | 领用单号，唯一 |
| asset_id | BIGINT | 是 | - | 资产 ID |
| user_id | BIGINT | 是 | - | 领用人 ID |
| department_id | BIGINT | 是 | - | 领用部门 ID |
| receive_date | TIMESTAMP WITH TIME ZONE | 是 | - | 领用日期 |
| reason | TEXT | 是 | - | 领用原因 |
| status | SMALLINT | 是 | - | 状态：0=待审批、1=已同意、2=已驳回、3=已领用、4=已归还 |
| approve_by | BIGINT | 否 | - | 审批人 ID |
| approve_time | TIMESTAMP WITH TIME ZONE | 否 | - | 审批时间 |
| approve_remark | TEXT | 否 | - | 审批备注 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | - | 删除标志 |

### 表名：{schema}.asset_return（资产归还确认表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| return_no | VARCHAR(100) | 是 | - | 归还单号，唯一 |
| receive_id | BIGINT | 是 | - | 关联领用单 ID |
| asset_id | BIGINT | 是 | - | 资产 ID |
| user_id | BIGINT | 是 | - | 归还人 ID |
| return_date | TIMESTAMP WITH TIME ZONE | 是 | - | 归还日期 |
| asset_status | SMALLINT | 是 | - | 归还时资产状态：0=正常、1=损坏、2=故障 |
| remark | TEXT | 否 | - | 归还备注 |
| confirm_by | BIGINT | 是 | - | 确认人 ID |
| confirm_time | TIMESTAMP WITH TIME ZONE | 是 | - | 确认时间 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | - | 删除标志 |

### 表名：{schema}.asset_transfer（资产调拨表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| transfer_no | VARCHAR(100) | 是 | - | 调拨单号，唯一 |
| asset_id | BIGINT | 是 | - | 资产 ID |
| out_dept_id | BIGINT | 是 | - | 调出部门 ID |
| in_dept_id | BIGINT | 是 | - | 调入部门 ID |
| out_user_id | BIGINT | 是 | - | 调出人 ID |
| in_user_id | BIGINT | 是 | - | 调入人 ID |
| transfer_date | TIMESTAMP WITH TIME ZONE | 是 | - | 调拨日期 |
| reason | TEXT | 是 | - | 调拨原因 |
| status | SMALLINT | 是 | - | 状态：0=待审批、1=已调拨、2=已驳回 |
| approve_by | BIGINT | 否 | - | 审批人 ID |
| approve_time | TIMESTAMP WITH TIME ZONE | 否 | - | 审批时间 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | - | 删除标志 |

### 表名：{schema}.asset_repair（资产维修表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| repair_no | VARCHAR(100) | 是 | - | 维修单号，唯一 |
| asset_id | BIGINT | 是 | - | 资产 ID |
| fault_desc | TEXT | 是 | - | 故障描述 |
| repair_desc | TEXT | 否 | - | 维修描述 |
| repair_user_id | BIGINT | 否 | - | 维修人 ID |
| repair_dept_id | BIGINT | 否 | - | 维修部门 ID |
| repair_file_url | TEXT | 否 | - | 维修附件 URL |
| repair_type | SMALLINT | 是 | - | 维修类型：0=送修、1=上门、2=远程 |
| vendor | VARCHAR(255) | 否 | - | 维修商 |
| cost | NUMERIC(12,2) | 否 | - | 维修费用 |
| apply_date | TIMESTAMP WITH TIME ZONE | 是 | - | 申请日期 |
| repair_date | TIMESTAMP WITH TIME ZONE | 否 | - | 维修日期 |
| finish_date | TIMESTAMP WITH TIME ZONE | 否 | - | 完成日期 |
| status | SMALLINT | 是 | - | 状态：0=待维修、1=维修中、2=已完成、3=无法维修 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | - | 删除标志 |

### 表名：{schema}.asset_scrap（资产报废表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| scrap_no | VARCHAR(100) | 是 | - | 报废单号，唯一 |
| asset_id | BIGINT | 是 | - | 资产 ID |
| reason | TEXT | 是 | - | 报废原因 |
| scrap_date | TIMESTAMP WITH TIME ZONE | 是 | - | 报废日期 |
| status | SMALLINT | 是 | - | 状态：0=待审批、1=已批准、2=已驳回、3=已报废 |
| approve_by | BIGINT | 否 | - | 审批人 ID |
| approve_time | TIMESTAMP WITH TIME ZONE | 否 | - | 审批时间 |
| handle_user | BIGINT | 否 | - | 处理人 ID |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | - | 删除标志 |

### 表名：{schema}.asset_purchase（资产采购申请表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| purchase_no | VARCHAR(100) | 是 | - | 采购单号，唯一 |
| asset_name | VARCHAR(255) | 是 | - | 采购资产名称 |
| category_id | BIGINT | 是 | - | 分类 ID |
| model | VARCHAR(255) | 否 | - | 型号 |
| manufacturer | VARCHAR(255) | 否 | - | 厂商 |
| quantity | INTEGER | 是 | - | 采购数量 |
| unit_price | NUMERIC(12,2) | 否 | - | 单价 |
| total_price | NUMERIC(12,2) | 否 | - | 总价 |
| apply_user | BIGINT | 是 | - | 申请人 ID |
| dept_id | BIGINT | 是 | - | 申请部门 ID |
| reason | TEXT | 是 | - | 采购原因 |
| status | SMALLINT | 是 | - | 状态：0=待审批、1=采购中、2=已完成、3=已驳回 |
| supplier | VARCHAR(255) | 否 | - | 供应商 |
| purchase_date | TIMESTAMP WITH TIME ZONE | 否 | - | 购买时间 |
| arrive_date | TIMESTAMP WITH TIME ZONE | 否 | - | 预计到货时间 |
| created_by | BIGINT | 否 | - | 创建人 ID |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |
| updated_by | BIGINT | 否 | - | 更新人 ID |
| updated_at | TIMESTAMP WITH TIME ZONE | 否 | - | 更新时间 |
| deleted | SMALLINT | 是 | - | 删除标志 |

### 表名：{schema}.asset_approval（通用审批记录表）

| 字段名 | 字段类型 | 是否非空 | 默认值 | 备注 |
|--------|---------|---------|-------|------|
| id | BIGINT | 是 | - | 主键 |
| biz_type | SMALLINT | 是 | - | 业务类型：1=领用、2=归还、3=调拨、4=维修、5=报废、6=采购 |
| biz_id | BIGINT | 是 | - | 业务 ID |
| step | SMALLINT | 是 | - | 审批步骤 |
| approver_id | BIGINT | 是 | - | 审批人 ID |
| approve_status | SMALLINT | 是 | - | 审批状态：0=待审、1=同意、2=驳回 |
| remark | TEXT | 否 | - | 审批备注 |
| approve_time | TIMESTAMP WITH TIME ZONE | 否 | - | 审批时间 |
| created_at | TIMESTAMP WITH TIME ZONE | 否 | - | 创建时间 |

# 7. 接口设计

## 7.1 接口架构

系统提供两种接口调用方式：

1. **Tauri Command（IPC）**：前端通过 `@tauri-apps/api` 的 `invoke` 方法调用 Rust 后端命令，适用于桌面应用内部通信；
2. **HTTP RESTful API（Axum）**：通过 HTTP 协议调用后端接口，支持 OpenAPI/Swagger 文档，适用于外部系统集成和调试。

两种接口共享相同的 Service 层业务逻辑，确保行为一致。

## 7.2 Tauri Command 接口列表

### 资产分类
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| get_categories | 获取分类列表 | GET /api/categories |
| get_categories_parents | 获取父分类列表 | GET /api/categories/parents |
| insert_category | 新增分类 | POST /api/categories |
| update_category | 编辑分类 | PUT /api/categories/{id} |
| delete_category | 删除分类 | DELETE /api/categories/{id} |

### 固定资产
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| get_hardware_assets | 获取固定资产列表 | GET /api/assets/hardware |
| insert_hardware_asset | 新增固定资产 | POST /api/assets/hardware |
| update_hardware_asset | 编辑固定资产 | PUT /api/assets/hardware/{id} |
| delete_hardware_asset | 删除固定资产 | DELETE /api/assets/hardware/{id} |

### 无形资产
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| get_intangible_assets | 获取无形资产列表 | GET /api/assets/intangible |
| insert_intangible_asset | 新增无形资产 | POST /api/assets/intangible |
| update_intangible_asset | 编辑无形资产 | PUT /api/assets/intangible/{id} |
| delete_intangible_asset | 删除无形资产 | DELETE /api/assets/intangible/{id} |

### 部门管理
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| get_departments | 获取部门列表 | GET /api/departments |
| insert_department | 新增部门 | POST /api/departments |
| update_department | 编辑部门 | PUT /api/departments/{id} |
| delete_department | 删除部门 | DELETE /api/departments/{id} |

### 用户管理
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| login | 用户登录 | POST /api/auth/login |
| get_users | 获取用户列表 | GET /api/users |
| insert_user | 新增用户 | POST /api/users |
| update_user | 编辑用户 | PUT /api/users/{id} |
| delete_user | 删除用户 | DELETE /api/users/{id} |
| get_current_user | 获取当前用户信息 | GET /api/users/me |
| reset_password | 重置密码 | POST /api/users/{id}/reset-password |

### 角色权限
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| get_roles | 获取角色列表 | GET /api/roles |
| insert_role | 新增角色 | POST /api/roles |
| delete_role | 删除角色 | DELETE /api/roles/{id} |
| get_role_menu_ids | 获取角色菜单 ID 列表 | GET /api/roles/{id}/menus |
| assign_role_menus | 分配角色菜单权限 | POST /api/roles/{id}/menus |
| get_all_menus_tree | 获取所有菜单树 | GET /api/menus/tree |
| get_user_menus | 获取用户菜单树 | GET /api/menus/user |
| get_user_role_ids | 获取用户角色 ID 列表 | GET /api/users/{id}/roles |
| assign_user_roles | 分配用户角色 | POST /api/users/{id}/roles |

### 流程管理
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| get_receives | 获取领用列表 | GET /api/process/receive |
| insert_receive | 新增领用 | POST /api/process/receive |
| update_receive | 编辑领用 | PUT /api/process/receive/{id} |
| delete_receive | 删除领用 | DELETE /api/process/receive/{id} |
| get_returns | 获取归还列表 | GET /api/process/return |
| insert_return | 新增归还 | POST /api/process/return |
| update_return | 编辑归还 | PUT /api/process/return/{id} |
| delete_return | 删除归还 | DELETE /api/process/return/{id} |
| get_transfers | 获取调拨列表 | GET /api/process/transfer |
| insert_transfer | 新增调拨 | POST /api/process/transfer |
| update_transfer | 编辑调拨 | PUT /api/process/transfer/{id} |
| delete_transfer | 删除调拨 | DELETE /api/process/transfer/{id} |
| get_repairs | 获取维修列表 | GET /api/process/repair |
| insert_repair | 新增维修 | POST /api/process/repair |
| update_repair | 编辑维修 | PUT /api/process/repair/{id} |
| delete_repair | 删除维修 | DELETE /api/process/repair/{id} |
| get_scraps | 获取报废列表 | GET /api/process/scrap |
| insert_scrap | 新增报废 | POST /api/process/scrap |
| update_scrap | 编辑报废 | PUT /api/process/scrap/{id} |
| delete_scrap | 删除报废 | DELETE /api/process/scrap/{id} |
| get_purchases | 获取采购列表 | GET /api/process/purchase |
| insert_purchase | 新增采购 | POST /api/process/purchase |
| update_purchase | 编辑采购 | PUT /api/process/purchase/{id} |
| delete_purchase | 删除采购 | DELETE /api/process/purchase/{id} |

### 知识库管理
| 命令名 | 功能 | HTTP 映射 |
|--------|------|----------|
| get_knowledge_tree | 获取知识树（返回树形结构） | GET /api/knowledge/tree |
| get_knowledge_tree_node | 获取指定节点详情 | GET /api/knowledge/tree/{id} |
| insert_knowledge_tree_node | 新增知识树节点 | POST /api/knowledge/tree |
| update_knowledge_tree_node | 编辑知识树节点 | PUT /api/knowledge/tree/{id} |
| delete_knowledge_tree_node | 删除知识树节点 | DELETE /api/knowledge/tree/{id} |
| move_knowledge_tree_node | 移动知识树节点（拖拽） | PUT /api/knowledge/tree/{id}/move |
| get_knowledge_document | 获取知识文档内容 | GET /api/knowledge/document/{id} |
| insert_knowledge_document | 新增知识文档 | POST /api/knowledge/document |
| update_knowledge_document | 编辑知识文档 | PUT /api/knowledge/document/{id} |
| delete_knowledge_document | 删除知识文档 | DELETE /api/knowledge/document/{id} |
| search_knowledge | 搜索知识库（关键词+语义） | GET /api/knowledge/search |
| get_knowledge_graph | 获取知识图谱数据 | GET /api/knowledge/graph |
| insert_knowledge_relation | 新增知识关联 | POST /api/knowledge/relation |
| delete_knowledge_relation | 删除知识关联 | DELETE /api/knowledge/relation/{id} |
| sync_knowledge_from_asset | 从资产同步知识 | POST /api/knowledge/sync/asset |
| sync_knowledge_from_document | 从合同同步知识 | POST /api/knowledge/sync/document |

## 7.3 HTTP API 认证

- 登录接口（POST /api/auth/login）为公开接口，无需认证；
- 其他所有 API 需要通过 Authorization 请求头携带 Bearer Token；
- Token 为 JWT 格式，包含用户 ID（sub）、用户名（username）、签发时间（iat）、过期时间（exp）；
- Token 过期时间默认为 24 小时。

## 7.4 API 响应格式

所有 API 返回统一格式：

```json
{
    "code": 200,
    "message": "success",
    "data": {}
}
```

错误响应格式：

```json
{
    "code": 401,
    "message": "无效的认证令牌",
    "data": null
}
```

## 7.5 OpenAPI 文档

- 系统启动后自动生成 OpenAPI 3.0 规范文档；
- Swagger UI 访问地址：`http://localhost:3001/api/swagger-ui/`；
- OpenAPI JSON 地址：`http://localhost:3001/api/openapi.json`；
- HTTP API 服务默认端口：3001（可通过环境变量 `API_PORT` 配置）。

# 8. 安全需求

## 8.1 认证安全

- 密码使用 Argon2 算法加密存储，不可逆；
- HTTP API 使用 JWT Bearer Token 认证，Token 包含过期时间；
- JWT 密钥通过环境变量 `JWT_SECRET` 配置，默认使用内置密钥（生产环境必须修改）。

## 8.2 数据安全

- 所有表支持逻辑删除（deleted 字段），避免物理删除导致数据丢失；
- 数据库连接信息通过 `.env.toml` 配置文件管理，不硬编码在代码中；
- 支持 PostgreSQL 读写分离，从库仅用于查询操作。

## 8.3 权限安全

- 基于 RBAC 模型，细粒度控制到按钮级别；
- 菜单表（sys_menu）同时存储 Tauri Command 名称和 HTTP 路由映射，支持双通道权限校验；
- 前端根据用户角色动态渲染菜单和按钮，后端接口校验用户权限。

## 8.4 前端安全

- 前端表单进行输入验证，防止 XSS 攻击；
- i64 类型 ID 序列化为字符串传输，防止前端 JavaScript 精度丢失。

# 9. 部署需求

## 9.1 环境要求

| 组件 | 要求 |
|------|------|
| 操作系统 | Windows 10+、macOS 12+、Linux（Ubuntu 20.04+） |
| PostgreSQL | 14+ |
| Node.js | 18+（仅开发环境需要） |
| Rust | 1.70+（仅开发环境需要） |

## 9.2 部署步骤

1. 安装 PostgreSQL 14+，创建数据库和用户；
2. 配置 `.env.toml` 文件，填写数据库连接信息；
3. 运行 Tauri 打包命令生成可执行文件；
4. 首次启动自动初始化数据库表结构和默认数据。

## 9.3 配置管理

系统配置通过 `.env.toml` 文件管理，支持以下配置段：

- `[postgres]`：PostgreSQL 连接配置（host、port、db_name、username、password、max_connections、min_connections）；
- `[postgres.read_replicas]`：从库配置（host、port、weight）；
- `[auth]`：认证配置（jwt_secret）；
- `[snowflake]`：雪花算法配置（worker_id）；
- `[api]`：HTTP API 配置（port）。

# 10. 测试需求

## 10.1 单元测试

- Rust 后端：使用 `#[tokio::test]` 编写异步单元测试，覆盖 Service 层核心业务逻辑；
- 密码哈希：测试 Argon2 加密和验证的正确性；
- 雪花算法：测试 ID 生成的唯一性和单调递增性。

## 10.2 集成测试

- 数据库操作测试：验证 SQL 语句的正确性和数据一致性；
- API 接口测试：验证 HTTP API 的请求/响应格式和状态码。

# 11. 维护需求

## 11.1 版本迭代

- 遵循语义化版本规范（SemVer）：主版本.次版本.修订号；
- 版本号在 `Cargo.toml` 中管理，通过 `script/update-version.js` 脚本统一更新。

## 11.2 日志维护

- 基于 OpenTelemetry 的日志系统，支持 JSON 格式输出；
- 日志级别通过环境变量 `RUST_LOG` 控制；
- 支持 OTLP 协议导出到 Jaeger、Prometheus、Grafana 等可观测性平台。

## 11.3 数据库维护

- 数据库迁移脚本存放在 `src-tauri/src/database/sql/` 目录；
- `public_tables.sql`：public schema 建表脚本；
- `public_migration.sql`：public schema 迁移脚本（补充缺失列）；
- `tenant_tables.sql`：租户 schema 建表脚本；
- 新增字段或表时，优先修改建表脚本，同时提供迁移脚本兼容已有数据库。
