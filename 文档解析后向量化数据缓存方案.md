# 文档解析后向量化数据缓存方案

## 1. 概述

本文档描述资产平台中文档解析后的向量化数据缓存架构方案。

### 核心原则

- **LanceDB 是主读写存储**（高性能本地向量数据库，读写快）
- **pgvector（PostgreSQL）是持久化归档**（数据备份、一致性校验、跨节点共享）
- **Valkey/Redis 是分布式缓存**（集群版，加速热点查询）
- **内存 LRU 是最顶层缓存**（极速响应重复查询）
- 写入时先写 LanceDB，再异步归档到 pgvector
- 查询时按 内存 → LanceDB → pgvector 三级路由

### 为什么 LanceDB 做主存储，pgvector 做归档？

| 维度 | LanceDB | pgvector |
|------|---------|----------|
| **写入性能** | ✅ 极快（列式存储 + 零拷贝） | ❌ 慢（B-Tree + IVFFlat 索引维护） |
| **查询性能** | ✅ 快（IVF-PQ 索引，磁盘+内存） | ⚠️ 中等（IVFFlat 近似索引） |
| **批量导入** | ✅ 原生支持大批量写入 | ❌ 大批量写入性能差 |
| **数据共享** | ❌ 本地文件 | ✅ 数据库，多节点可共享 |
| **事务/回滚** | ⚠️ 有限 | ✅ 完整 ACID |
| **备份恢复** | ⚠️ 文件级 | ✅ pg_dump/pg_restore |

### 部署模式

| 模式 | 适用场景 | 存储架构 |
|------|---------|---------|
| **桌面版** | 单机 Tauri 桌面应用 | LanceDB（主）+ 内存 LRU + pgvector（归档） |
| **集群版** | 前后端分离 Web 部署 | LanceDB（主）+ Valkey/Redis（缓存）+ 内存 LRU + pgvector（归档） |

---

## 2. 架构总览

```
                    ┌─────────────────────────────────────┐
                    │         文档解析流水线                │
                    │  (PDF/DOCX/MD → 分块 → Embedding)   │
                    └──────────────┬──────────────────────┘
                                   │ 写入（同步，高性能）
                                   ▼
              ┌──────────────────────────────────────────┐
              │              LanceDB                      │ ◄── 主读写存储
              │         (本地磁盘向量数据库)               │     (Primary Store)
              │    读写快，支持 ANN + 过滤 + 批量          │
              └──────────┬───────────────────────────────┘
                         │ 异步归档（后台任务）
                         ▼
              ┌──────────────────────────────────────────┐
              │      PostgreSQL (pgvector 扩展)           │ ◄── 持久化归档
              │  asset_knowledge.vector_data (vector[])   │     (Archive / Backup)
              │  数据备份、一致性校验、跨节点共享           │
              └──────────────────────────────────────────┘
                         │ 缓存同步
                         ▼
              ┌──────────────────────────────────────────┐
              │              缓存层                       │
              │  ┌─────────────────┐  ┌────────────────┐ │
              │  │   内存 LRU      │  │ Valkey/Redis   │ │
              │  │  (最顶层热点)    │  │ (分布式缓存)    │ │
              │  │  桌面版/集群版   │  │  仅集群版使用   │ │
              │  └─────────────────┘  └────────────────┘ │
              └──────────────────────────────────────────┘
```

---

## 3. 数据模型

### 3.1 LanceDB（主存储）

**存储位置**：`{data_dir}/lancedb/`（Tauri 应用数据目录）

**表结构**：

```python
import lancedb
import pyarrow as pa

schema = pa.schema([
    pa.field("id", pa.int64()),                    # 知识条目 ID（主键）
    pa.field("vector", pa.list_(pa.float32(), 768)),  # 向量数据
    pa.field("title", pa.string()),               # 标题
    pa.field("content", pa.string()),             # 完整内容（用于展示）
    pa.field("content_preview", pa.string()),     # 内容预览（前 200 字）
    pa.field("permission_level", pa.string()),    # 权限等级
    pa.field("doc_source", pa.string()),          # 数据来源
    pa.field("knowledge_type", pa.string()),      # 知识类型
    pa.field("asset_id", pa.int64()),             # 关联资产 ID
    pa.field("chunk_index", pa.int32()),          # 分块序号
    pa.field("created_at", pa.timestamp('us')),   # 创建时间
    pa.field("updated_at", pa.timestamp('us')),   # 更新时间
    pa.field("deleted", pa.int16()),              # 删除标志
])
```

**索引**：创建 IVF-PQ 索引加速 ANN 检索

```python
# 创建索引
table.create_index(
    metric="cosine",          # 余弦相似度
    num_partitions=256,       # IVF 分区数
    num_sub_vectors=96,       # PQ 子向量数
)
```

### 3.2 PostgreSQL (pgvector) — 归档层

**现有表**：`{schema}.asset_knowledge`

**改造**：将 `vector_data` 从 `BYTEA` 改为 pgvector 的 `vector` 类型

```sql
-- 启用 pgvector 扩展
CREATE EXTENSION IF NOT EXISTS vector;

-- 修改字段类型（以 768 维为例，根据实际 Embedding 模型调整）
ALTER TABLE {schema}.asset_knowledge 
  ALTER COLUMN vector_data TYPE vector(768) 
  USING vector_data::text::vector;

-- 创建 IVFFlat 索引（加速归档层查询，仅兜底时使用）
CREATE INDEX IF NOT EXISTS idx_knowledge_vector 
  ON {schema}.asset_knowledge 
  USING ivfflat (vector_data vector_cosine_ops) 
  WITH (lists = 100);
```

**向量维度说明**：

| 模型 | 维度 | 适用场景 |
|------|------|---------|
| all-MiniLM-L6-v2 | 384 | 轻量级，速度快 |
| bge-base-en-v1.5 | 768 | 平衡性能与精度 |
| bge-large-zh-v1.5 | 1024 | 中文场景推荐 |
| text-embedding-3-small | 1536 | OpenAI 方案 |
| text-embedding-3-large | 3072 | 高精度场景 |

### 3.3 Valkey/Redis（集群版）

**选型说明**：本项目推荐使用 **Valkey** 作为分布式缓存，Redis 作为备选方案。两者协议兼容，代码层面无需改动。

#### Valkey vs Redis 对比

| 维度 | Valkey 🏆 | Redis |
|------|----------|-------|
| **开源协议** | BSD-3-Clause（完全开放） | RSALv2 + SSPL（非严格开源） |
| **维护方** | Linux 基金会（社区中立） | Redis Ltd.（商业公司） |
| **API 兼容性** | 100% 兼容 Redis 协议 | — |
| **数据结构** | 全部支持（String/Hash/List/Set/ZSet/Stream） | 全部支持 |
| **集群模式** | ✅ 原生支持 | ✅ 原生支持 |
| **性能** | 与 Redis 7.2 持平 | 基准 |
| **新特性** | 持续跟进社区需求 | 由 Redis Ltd. 控制路线图 |
| **Rust 客户端** | `redis` crate 直接可用（协议兼容） | `redis` crate |
| **Docker 镜像** | `valkey/valkey` | `redis/redis` |

> **结论**：Valkey 是 Redis 在 2024 年 BSD 协议分叉后的社区版本，由 Linux 基金会托管，协议更开放、社区更中立。两者协议完全兼容，Rust 的 `redis` crate 可直接连接 Valkey，代码零改动。推荐使用 Valkey，备选 Redis。

#### Key 设计

```
# 向量缓存（二进制序列化）
VECTOR:{knowledge_id} → 二进制 float32 数组

# 搜索结果缓存（TTL: 300 秒）
SEARCH:{md5(query)}:{top_k} → JSON [{id, score, title, permission_level}]

# 元数据缓存
META:{knowledge_id} → JSON {title, permission_level, doc_source, knowledge_type}

# 倒排索引（关键词 → 知识条目 ID 集合）
INVERTED:{keyword} → Set<knowledge_id>

# 最近访问时间（用于 LRU 淘汰）
ACCESS_TIME:{knowledge_id} → Unix 时间戳
```

### 3.4 内存缓存（moka）

**缓存框架**：[moka](https://github.com/moka-rs/moka) — 高性能并发 LRU/LFU 缓存库

moka 是 Rust 生态中最成熟的高性能缓存库之一，支持多种淘汰策略和并发访问，适合作为本项目的顶层热点缓存。

#### 核心特性

| 特性 | 说明 |
|------|------|
| **淘汰策略** | LRU（最近最少使用）、LFU（最不经常使用）、Segmented LRU (SLRU) |
| **并发安全** | 基于 `crossbeam` + `dashmap`，无锁读、细粒度锁写 |
| **TTL/TTI** | 支持过期时间（Time-To-Live）和空闲时间（Time-To-Idle） |
| **异步加载** | 支持 `async` 方式按需加载缓存值 |
| **事件监听** | 支持淘汰、过期、移除等事件的回调通知 |
| **统计指标** | 内置命中率、访问次数等统计 |
| **两种变体** | `sync::Cache`（同步）和 `future::Cache`（异步） |

#### 数据结构

```rust
use moka::sync::Cache;
use std::time::Duration;

/// 向量缓存管理器
struct VectorCache {
    // 热点向量缓存：最多 1000 条，5 分钟无访问自动过期
    vectors: Cache<i64, CachedVector>,
}

/// 缓存的向量条目
#[derive(Clone)]
struct CachedVector {
    id: i64,
    vector: Vec<f32>,
    title: String,
    permission_level: String,
    score: f32,  // 与查询的相似度
}

impl VectorCache {
    /// 创建默认配置的缓存
    fn new(max_capacity: u64) -> Self {
        Self {
            vectors: Cache::builder()
                .max_capacity(max_capacity)     // 最大条目数
                .time_to_idle(Duration::from_secs(300))  // 5 分钟无访问则过期
                .build(),
        }
    }

    /// 获取缓存（更新 LRU 顺序）
    fn get(&self, id: &i64) -> Option<CachedVector> {
        self.vectors.get(id)
    }

    /// 查看缓存（不更新 LRU 顺序）
    fn peek(&self, id: &i64) -> Option<CachedVector> {
        self.vectors.peek(id)
    }

    /// 写入缓存
    fn insert(&self, id: i64, value: CachedVector) {
        self.vectors.insert(id, value);
    }

    /// 删除缓存
    fn invalidate(&self, id: &i64) {
        self.vectors.invalidate(id);
    }

    /// 清空所有缓存
    fn clear(&self) {
        self.vectors.invalidate_all();
    }
}
```

#### 常用 API

```rust
// ========== 构建缓存 ==========

// 基础 LRU 缓存
let cache: Cache<u64, String> = Cache::new(100);

// 带 TTL 的缓存
let cache = Cache::builder()
    .max_capacity(1000)
    .time_to_live(Duration::from_secs(600))   // 10 分钟过期
    .time_to_idle(Duration::from_secs(300))   // 5 分钟无访问过期
    .build();

// 异步缓存（需要 features = ["future"]）
let cache: future::Cache<u64, String> = future::Cache::builder()
    .max_capacity(1000)
    .time_to_live(Duration::from_secs(600))
    .build();

// ========== 基本操作 ==========

cache.insert(1, "value".to_string());           // 写入
let v = cache.get(&1);                          // 读取（更新 LRU 顺序）
let v = cache.peek(&1);                         // 读取（不更新 LRU 顺序）
cache.invalidate(&1);                           // 删除单个
cache.invalidate_all();                         // 清空所有

// ========== 获取或初始化（同步） ==========

let value = cache.get_with(1, || {
    "computed_value".to_string()
});

// ========== 获取或初始化（异步） ==========

let value = cache.get_with(1, async {
    some_async_function().await
}).await;

// ========== 尝试获取（不阻塞） ==========

if let Some(v) = cache.get(&1) {
    // 缓存命中
} else {
    // 缓存未命中，从下层存储加载
}

// ========== 统计信息 ==========

let stats = cache.entry_count();                // 当前条目数
// 通过 weigher 可以统计权重（如字节数）
```

#### 高级配置

```rust
use moka::sync::Cache;
use std::time::Duration;

let cache: Cache<String, Vec<f32>> = Cache::builder()
    // 最大容量（条目数）
    .max_capacity(10_000)
    // 按权重限制（例如按字节数）
    .weigher(|_key, value: &Vec<f32>| -> u32 {
        value.len() as u32  // 每个向量维度数作为权重
    })
    .max_capacity(1_000_000)  // 最大权重总和
    // 过期策略
    .time_to_live(Duration::from_secs(3600))    // 1 小时强制过期
    .time_to_idle(Duration::from_secs(600))     // 10 分钟无访问过期
    // 淘汰/过期回调
    .eviction_listener(|key, value, cause| {
        tracing::debug!("缓存淘汰: key={:?}, cause={:?}", key, cause);
    })
    // 初始化容量（减少扩容开销）
    .initial_capacity(1000)
    .build();
```

#### 淘汰策略对比

| 策略 | moka 对应 | 适用场景 |
|------|----------|---------|
| **LRU** | 默认（`Cache::new()`） | 通用场景，最近访问的数据最可能被再次访问 |
| **Segmented LRU** | `Cache::builder()` 默认 | moka 内部实现，比纯 LRU 更好的命中率 |
| **LFU** | 暂不直接支持 | 访问频率比访问时间更重要的场景 |
| **TTL** | `.time_to_live()` | 数据有时效性，过期后必须重新加载 |
| **TTI** | `.time_to_idle()` | 数据在空闲一段时间后失效 |

> moka 内部默认使用 **Segmented LRU (SLRU)** 策略，它将缓存分为 probation 和 protected 两个区域，新条目先进入 probation 区，被多次访问后晋升到 protected 区，比纯 LRU 有更好的命中率。

#### 在项目中的使用

```rust
// cache/memory_cache.rs — 内存缓存实现

use moka::sync::Cache;
use std::time::Duration;

/// 内存缓存配置
#[derive(Clone)]
pub struct MemoryCacheConfig {
    pub max_capacity: u64,           // 最大条目数
    pub ttl_seconds: u64,            // 过期时间（秒）
    pub tti_seconds: u64,            // 空闲过期时间（秒）
}

impl Default for MemoryCacheConfig {
    fn default() -> Self {
        Self {
            max_capacity: 1000,
            ttl_seconds: 3600,       // 1 小时
            tti_seconds: 300,        // 5 分钟
        }
    }
}

/// 内存缓存管理器
pub struct MemoryCache {
    vectors: Cache<i64, CachedVector>,
    search_results: Cache<String, Vec<SearchResult>>,
    config: MemoryCacheConfig,
}

impl MemoryCache {
    pub fn new(config: MemoryCacheConfig) -> Self {
        Self {
            vectors: Cache::builder()
                .max_capacity(config.max_capacity)
                .time_to_live(Duration::from_secs(config.ttl_seconds))
                .time_to_idle(Duration::from_secs(config.tti_seconds))
                .eviction_listener(|key, _value, cause| {
                    tracing::debug!("向量缓存淘汰: id={}, cause={:?}", key, cause);
                })
                .build(),
            search_results: Cache::builder()
                .max_capacity(config.max_capacity / 2)
                .time_to_live(Duration::from_secs(60))  // 搜索结果缓存 1 分钟
                .build(),
            config,
        }
    }

    /// 获取向量缓存
    pub fn get_vector(&self, id: &i64) -> Option<CachedVector> {
        self.vectors.get(id)
    }

    /// 写入向量缓存
    pub fn set_vector(&self, id: i64, vector: CachedVector) {
        self.vectors.insert(id, vector);
    }

    /// 获取搜索结果缓存
    pub fn get_search_results(&self, key: &str) -> Option<Vec<SearchResult>> {
        self.search_results.get(key)
    }

    /// 写入搜索结果缓存
    pub fn set_search_results(&self, key: String, results: Vec<SearchResult>) {
        self.search_results.insert(key, results);
    }

    /// 失效指定向量缓存
    pub fn invalidate_vector(&self, id: &i64) {
        self.vectors.invalidate(id);
    }

    /// 获取缓存统计
    pub fn stats(&self) -> CacheStats {
        CacheStats {
            vector_count: self.vectors.entry_count(),
            search_count: self.search_results.entry_count(),
        }
    }
}

#[derive(Debug)]
pub struct CacheStats {
    pub vector_count: u64,
    pub search_count: u64,
}
```

#### 性能对比

| 缓存库 | 并发读 | 并发写 | 内存开销 | 编译时间 |
|--------|--------|--------|---------|---------|
| **moka** | ✅ 极高（无锁读） | ✅ 高（分段锁） | 中等 | 较慢（依赖多） |
| **lru** | ❌ 需外部加锁 | ❌ 需外部加锁 | 低 | 快 |
| **quick_cache** | ✅ 高 | ✅ 高 | 低 | 快 |

> **选择建议**：本项目使用 moka 作为顶层热点缓存，因为需要并发安全（Tokio 多线程运行时）和高命中率（SLRU 策略）。如果对编译速度敏感，可考虑 `quick_cache` 作为替代。

---

## 4. 数据流

### 4.1 写入流程

```
文档解析完成 → 分块 → Embedding
   │
   ├── ① LanceDB — 同步写入（主存储，高性能）
   │      table.add([record]) 或 table.merge_insert()
   │
   ├── ② 内存缓存 — 同步更新（如果是热点）
   │
   └── ③ 异步归档（Tokio::spawn，低优先级）
          └── PostgreSQL pgvector — 异步写入
                 INSERT INTO asset_knowledge (...) VALUES (...)
```

### 4.2 查询流程

```
用户查询（文本）
   │
   ├── ① Embedding 模型 → 查询向量
   │
   ├── ② 内存缓存查询
   │     ├── 命中 → 直接返回结果（< 1μs）
   │     └── 未命中 → 继续
   │
   ├── ③ LanceDB 查询（主存储，桌面版）
   │     ├── 命中 → 返回结果 + 异步回填内存缓存（< 5ms）
   │     └── 未命中 → 继续
   │
   ├── ④ Valkey/Redis 查询（集群版）
   │     ├── 命中 → 返回结果 + 异步回填内存缓存（< 1ms）
   │     └── 未命中 → 继续
   │
   └── ⑤ PostgreSQL pgvector 查询（归档兜底）
         └── 返回结果 + 异步回填 LanceDB + 内存（< 20ms）
```

### 4.3 更新流程

```
更新知识条目
   │
   ├── ① LanceDB — 同步更新（主存储）
   │      table.update().where("id = ?").set(...)
   │
   ├── ② 内存缓存 — 同步更新/失效
   │
   └── ③ 异步归档
          └── PostgreSQL pgvector — 异步更新
```

### 4.4 删除流程

```
删除知识条目（软删除）
   │
   ├── ① LanceDB — 同步软删除
   │      table.update().where("id = ?").set("deleted", 1)
   │
   ├── ② 内存缓存 — 同步失效
   │
   └── ③ 异步归档
          └── PostgreSQL pgvector — 异步软删除
```

---

## 5. 同步策略

### 5.1 全量同步（pgvector ← LanceDB）

| 触发时机 | 方向 | 说明 |
|---------|------|------|
| 应用启动时 | LanceDB → pgvector | 检查 pgvector 是否为空，为空则全量归档 |
| 每日定时（可选） | LanceDB → pgvector | 凌晨低峰期全量同步，保证归档层完整 |
| 手动触发 | LanceDB → pgvector | 管理后台"归档到数据库"按钮 |

### 5.2 增量同步

| 操作 | 主存储 | 归档层 | 说明 |
|------|--------|--------|------|
| INSERT | LanceDB 同步 | pgvector 异步 | 写入 LanceDB 后立即异步归档 |
| UPDATE | LanceDB 同步 | pgvector 异步 | 更新 LanceDB 后立即异步归档 |
| DELETE | LanceDB 同步 | pgvector 异步 | 软删除后立即异步归档 |

### 5.3 一致性保证

- **最终一致性**：LanceDB 是主存储，pgvector 允许短暂落后（秒级）
- **版本号**：每条记录带 `updated_at` 时间戳，归档时比较版本
- **兜底查询**：LanceDB 未命中时回源到 pgvector，保证数据完整
- **启动校验**：应用启动时可对比 LanceDB 和 pgvector 的记录数，不一致则触发全量同步

---

## 6. Rust 后端实现

### 6.1 模块结构

```
src-tauri/src/
├── cache/
│   ├── mod.rs              # 缓存管理器（统一入口）
│   ├── memory_cache.rs     # 内存 LRU 缓存（moka）
│   ├── lancedb_store.rs    # LanceDB 主存储封装
│   └── valkey_cache.rs     # Valkey/Redis 缓存（集群版，可选编译）
├── vector/
│   ├── mod.rs
│   ├── pgvector.rs         # pgvector 归档层封装
│   ├── embedding.rs        # Embedding 客户端
│   └── sync.rs             # 缓存同步逻辑
```

### 6.2 新增依赖（Cargo.toml）

```toml
# LanceDB（主存储）
lancedb = { version = "0.16", optional = true }

# 内存缓存
moka = { version = "0.12", features = ["future"] }

# Valkey/Redis（集群版，可选）
redis = { version = "0.27", features = ["tokio-comp", "json"], optional = true }

# 特征标记
[features]
default = ["lancedb-store"]
lancedb-store = ["lancedb"]
valkey-cache = ["redis"]
```

### 6.3 核心接口

```rust
// cache/mod.rs — 缓存管理器

pub enum StoreLayer {
    Memory,
    LanceDB,         // 主存储
    Valkey,          // 集群版缓存
    PgVector,        // 归档兜底
}

pub struct VectorStore {
    memory: MemoryCache,
    lancedb: LanceDBStore,              // 主存储（必选）
    valkey: Option<ValkeyCache>,        // 集群版（可选）
    pgvector: PgVectorArchive,          // 归档层（必选）
}

impl VectorStore {
    /// 查询向量（按层级依次查找）
    pub async fn search(
        &self,
        query_vector: &[f32],
        top_k: usize,
        filters: &SearchFilters,
    ) -> Result<Vec<SearchResult>> {
        // 1. 内存缓存查询
        // 2. LanceDB 查询（主存储）
        // 3. Valkey/Redis 查询（集群版）
        // 4. pgvector 归档兜底
        // 5. 回填缓存
    }

    /// 写入向量（先写 LanceDB，再异步归档 pgvector）
    pub async fn insert(
        &self,
        knowledge: &AssetKnowledge,
        vector: &[f32],
    ) -> Result<()> {
        // 1. 写入 LanceDB（主存储，同步）
        // 2. 更新内存缓存（同步）
        // 3. 异步归档到 pgvector（后台任务）
    }

    /// 删除向量
    pub async fn delete(&self, id: i64) -> Result<()> {
        // 1. LanceDB 软删除（同步）
        // 2. 内存缓存失效（同步）
        // 3. 异步归档 pgvector 软删除
    }
}
```

---

## 7. 前端适配

### 7.1 搜索接口

```typescript
// services/knowledgeService.ts

/** 向量搜索知识条目 */
export function searchKnowledge(params: {
    query: string;          // 搜索文本
    top_k?: number;         // 返回数量（默认 10）
    permission_level?: string;  // 权限过滤
    knowledge_type?: string;    // 知识类型过滤
}) {
    return api.post<SearchResult[]>('search_knowledge', params);
}

export interface SearchResult {
    id: string;
    title: string;
    content_preview: string;
    score: number;          // 相似度分数
    permission_level: string;
    knowledge_type: string;
    doc_source: string;
}
```

---

## 8. 部署配置

### 8.1 桌面版（默认）

```toml
# .env 或 config.toml
[store]
# 主存储：lancedb（默认）
primary = "lancedb"

# LanceDB 存储路径（默认：{app_data_dir}/lancedb）
lancedb_path = ""

# 内存缓存大小（条数）
memory_cache_size = 1000

# pgvector 归档连接（可选，桌面版可以不配）
[archive]
pg_host = "localhost"
pg_port = 5432
pg_database = "assets_platform"
```

### 8.2 集群版

```toml
[store]
primary = "lancedb"

# LanceDB 存储路径
lancedb_path = "/data/lancedb"

# 内存缓存大小
memory_cache_size = 2000

# Valkey/Redis 分布式缓存
[cache]
valkey_url = "valkey://localhost:6379"
search_cache_ttl = 300

# pgvector 归档
[archive]
pg_host = "postgres-host"
pg_port = 5432
pg_database = "assets_platform"
```

---

## 9. 性能预期

| 存储/缓存层 | 延迟 | 命中率（预估） | 容量 |
|------------|------|---------------|------|
| 内存 LRU | < 1μs | 60-80%（热点数据） | 1000 条 |
| LanceDB | < 5ms | 95%+（主存储） | 全量数据 |
| Valkey/Redis（集群版） | < 1ms | 70-90%（含内存） | 全量数据 |
| pgvector（归档兜底） | < 20ms | < 1%（极少触发） | 全量数据 |

---

## 10. 实施计划

### 第一阶段：LanceDB 主存储
- [ ] 集成 LanceDB Rust SDK
- [ ] 实现 LanceDB 存储封装（建表、写入、检索、删除、批量）
- [ ] 创建 IVF-PQ 索引
- [ ] 实现 Embedding 客户端（对接 Ollama/OpenAI 兼容 API）

### 第二阶段：内存缓存
- [ ] 集成 moka 缓存库
- [ ] 实现 LRU 淘汰策略
- [ ] 实现查询结果回填

### 第三阶段：pgvector 归档
- [ ] 启用 pgvector 扩展，修改 `vector_data` 字段类型
- [ ] 实现 pgvector 归档写入（异步）
- [ ] 实现 pgvector 归档查询（兜底）
- [ ] 实现启动时全量同步校验

### 第四阶段：Valkey/Redis 缓存（集群版）
- [ ] 集成 Valkey/Redis 客户端（可选编译）
- [ ] 实现 Valkey/Redis 向量缓存 + 搜索结果缓存
- [ ] 实现 Valkey/Redis 倒排索引
- [ ] 实现配置切换：桌面版 ↔ 集群版

---

## 11. 注意事项

1. **向量维度一致性**：所有层的向量维度必须一致，否则检索结果错误
2. **权限过滤**：所有查询时必须带上 `permission_level` 过滤条件
3. **软删除同步**：LanceDB 软删除后必须同步清理内存缓存和归档层
4. **缓存预热**：应用启动时从 LanceDB 加载热点数据到内存缓存
5. **监控指标**：记录各层的命中率、延迟，用于调优
6. **LanceDB 文件备份**：定期备份 `{data_dir}/lancedb/` 目录
7. **pgvector 重建**：归档层数据损坏时，可从 LanceDB 全量重建
